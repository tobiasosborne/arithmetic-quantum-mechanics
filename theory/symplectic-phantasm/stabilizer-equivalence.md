# Stabilizer membership, fullness, and the convention bridge

Lane model: `gpt-5.6-sol`, reasoning `xhigh`.

Admitted proof component for `SP-STAB-REL`; see
`theory/verdicts/phantasm-relations-adjudication.md`. This shard completes the constructive equivalence
after `stabilizer-intertwiner-line.md` and `stabilizer-functor-laws.md`.  It uses every second-level
unitary from D1307, rather than checking only named Clifford generators.  It
also isolates the source comparison: SP-CK21 `lagrel.tex` 3647--3744 states
only a symmetric monoidal equivalence, while SP-BC24
`section_AffLagRel.tex` 143--177 uses a time-reversed dagger.  Neither source
dagger is substituted for D1702's bare converse.

Use the companion shards' proof-local abbreviations.  All canonical formulas
below use the existing fixed `psi_(F_p)`, `H_(F_p,n)`, and
`W^s_(F_p,n)` of D1301/D1703.

## 1. Opposite-space vectorization

**ASSUME** a standard space `V_m=F_p^m+F_p^m`.  Define, for this proof,

    c_m(a,b)=(a,-b),
    vec_(m,n)(T)=sum_(x in F_p^m) delta_x tensor T delta_x.

**PROVE** vectorization turns the D1715 operator line into the ordinary
stabilizer-state line of a standard affine Lagrangian state.

<1>1. The map `c_m:bar(V_m)->V_m` is symplectic because

    omega_m(c_m(a,b),c_m(a',b'))=-omega_m((a,b),(a',b')).

**BY** direct substitution in D1703's standard form.

<1>2. Entrywise complex conjugation in the computational basis satisfies

    overline(W^s_(F_p,m)(a,b))=W^s_(F_p,m)(a,-b).

**BY** conjugate D1703's wavefunction formula; `overline(psi(x))=psi(-x)`
reverses `b` and leaves the translation `a` fixed.

<1>3. For operators `A,B,T` of compatible sizes,

    vec(A T B^*)=(overline B tensor A)vec(T).

**BY** expand both sides on the computational basis and use
`B^* delta_j=sum_i overline(B_(j i))delta_i`.

<1>4. Combining `<1>2`--`<1>3` gives

    vec(W_n(w)T W_m(v)^*)
      =(W_m(c_m v) tensor W_n(w))vec(T).

**BY** those steps and D1704's factor order.

<1>5. For nonempty `R=r+L:V_m->V_n`, put

    Q_R=(c_m+1_(V_n))(R) subset V_m+V_n.

This is an affine Lagrangian state after the SP-COMPACT name changes the
type from `V_m->V_n` to `0->bar(V_m)+V_n`.
**BY** the explicit SP-COMPACT dependency, `<1>1`, and preservation of
Lagrangian affine subspaces by a symplectic isomorphism.

<1>6. If `q=(c_m r_m,r_n)` and `k=(c_m v,w)`, then

    omega_(m+n)(q,k)=Omega_(m,n)(r,(v,w)).

**BY** `<1>1`, D1701's direct-sum form, and the definition of `Omega`.

<1>7. Therefore `T in E_p(R)` exactly when `vec(T)` obeys

    W^s_(F_p,m+n)(k)vec(T)
      =psi_(F_p)(-omega_(m+n)(q,k))vec(T)

for all `q in Q_R` used as origins and all `k in Q_R-Q_R`.
**BY** D1715, `<1>4`--`<1>6`, and admitted SP-TENSOR's exact grouped Weyl
identification.

<1>8. The map `vec` is a linear isomorphism, with inverse determined by
matrix entries.
**BY** it maps the matrix units to the standard tensor basis.

<1>9. **QED** the explicit opposite-form, factor-order, and vectorization
bridge.
**BY** `<1>1`--`<1>8`.

## 2. Every line contains actual stabilizer amplitudes

**ASSUME** a nonempty affine Lagrangian state `Q=q+K` in the standard
space `V_j`.  **PROVE** its unique Weyl eigenline is generated by a D1704
stabilizer amplitude `C->H_(F_p,j)`.

<1>1. Let

    B_j={(0,b):b in F_p^j} subset V_j.

Then `B_j` is Lagrangian and `delta_0^(tensor j)` obeys
`W^s_(F_p,j)(0,b)delta_0^(tensor j)=delta_0^(tensor j)`.
**BY** D1703's standard form and wavefunction formula; tensor the
one-register computational preparation from D1704.

<1>2. There is a symplectic linear isomorphism `g` of `V_j` with
`g(B_j)=K`.

  <2>1. Choose a basis `b_1,...,b_j` of `K`.  The map
  `V_j->K^*`, `x |-> omega_j(-,x)|_K`, is onto with kernel `K`, so choose
  `d_i` with `omega_j(b_i,d_h)=delta_(ih)`.
  **BY** `K=K^perp`, rank-nullity, and nondegeneracy.

  <2>2. Put `A_(ih)=omega_j(d_i,d_h)` and

      c_i=d_i-(1/2)sum_h A_(ih)b_h.

  Then `omega_j(b_i,c_h)=delta_(ih)` and `omega_j(c_i,c_h)=0`.
  **BY** bilinearity, alternation `A_(hi)=-A_(ih)`, and direct substitution;
  `2` is invertible because `p` is odd.

  <2>3. Thus `(b_i,c_i)` is a symplectic basis.  Sending the analogous
  standard basis, with its first half the vertical basis of `B_j` and its
  companion horizontal vectors signed so the pairing is `+delta_(ih)`, to
  this basis defines the required `g`.
  **BY** `<2>1`--`<2>2` and equality of the two displayed pairing matrices.

  <2>4. **QED** `<1>2`.

<1>3. SP-EGOROV supplies a unitary projective implementer `U_(q,g)` on the
standard model satisfying

    U W(x) U^*=psi_(F_p)(omega_j(q,gx))W(gx).

**BY** admitted SP-EGOROV and SP-WEYL.

<1>4. This implementer belongs to D1307's `C_2(A)` because it sends every
`lambda W(x)` in the full-scalar Pauli group to another scalar multiple of
a Weyl operator.
**BY** D1307 and `<1>3`, for every Pauli element, including arbitrary
`lambda in U(1)`.

<1>5. Rearranging `<1>3` and applying it to the reference state gives

    W(gb)U delta_0^(tensor j)
      =psi_(F_p)(-omega_j(q,gb))U delta_0^(tensor j)

for every `b in B_j`.
**BY** `<1>1`--`<1>3`.

<1>6. Hence `U delta_0^(tensor j)` lies in the state line for `Q`; it is
nonzero because `U` is unitary.
**BY** `K=gB_j`, `<1>5`, and `stabilizer-intertwiner-line.md` section 2, which makes
the one-origin equations equivalent to D1715's all-origin prescription.

<1>7. D1704 contains this vector as an actual map: it contains every
`C_2(A)` unitary, the one-register `delta_0`, tensor, and composition.
**BY** D1704 and `<1>4`.

<1>8. The state-line group-average calculation is the `m=0,n=j` case of
`stabilizer-intertwiner-line.md` section 3, so the line is one-dimensional.  Every
vector in it is a complex scalar multiple of the D1704 map in `<1>7`.
**BY** that line theorem and D1704's scalar maps.

<1>9. **QED** every nonempty affine Lagrangian state line consists of
actual stabilizer amplitudes.
**BY** `<1>6`--`<1>8`.

**ASSUME** now nonempty `R:V_m->V_n` and `T in E_p(R)`.  **PROVE** every
such `T` is in `Stab_p^amp(H_(F_p,m),H_(F_p,n))`.

<1>10. Section 1 makes `vec(T)` an element of the affine Lagrangian state
line for `Q_R`; section 2 `<1>9` makes it an actual stabilizer state.
**BY** sections 1--2.

<1>11. Put `Omega_m=sum_x delta_x tensor delta_x=vec(1_(H_m))`.
The same state argument applied to the affine Lagrangian state `Q_(Delta)`
shows `Omega_m:C->H_m tensor H_m` is a D1704 amplitude; therefore its
adjoint is also a D1704 amplitude.
**BY** `<1>9`, section 1 with `R=Delta_(V_m)`, and D1704's adjoint closure.

<1>12. Direct basis evaluation gives the unvectorization formula

    T=(<Omega_m| tensor 1_(H_n))
        o (1_(H_m) tensor vec(T)),

with D1704's canonical associator and unit identifications understood.
**BY** on input `delta_y`, the Bell effect kills every summand except
`x=y` and leaves `T delta_y`.

<1>13. The right side is generated in D1704, so `T` is an actual
stabilizer amplitude.  Since D1704 contains all scalar maps, the whole line
`E_p(R)` is contained in the stated Hom-set.
**BY** `<1>10`--`<1>12` and D1704.

<1>14. For every `j>=1`, let `e_j:C->H_(F_p,j)` be the tensor product of
`j` copies of D1704's one-register zero preparation, and put
`e_0=1_C`.  D1704 also contains every `e_j^*` and the scalar generator
`0:C->C`.  Hence, for every pair `m,n`, it contains the typed composite

    e_n o (0:C->C) o e_m^*:H_(F_p,m)->H_(F_p,n),

which is the zero linear map.  This is the sole element prescribed by
`E_p(empty)`.
**BY** D1704's preparation, scalar, tensor, composition and adjoint
generators, D1715's empty prescription, and direct evaluation through the
middle zero map.

<1>15. **QED** the target-membership clause.
**BY** `<1>13`--`<1>14`.

## 3. Every D1307 second-level unitary is an affine graph line

**ASSUME** an arbitrary `U in C_2(A)` on `H_(F_p,n)`.  **PROVE** its
projective class is the line of the graph of one affine symplectic
isomorphism.

<1>1. For each Weyl label `v`, D1307 supplies a phase `gamma(v) in U(1)`
and a unique label `g(v)` such that

    U W(v) U^*=gamma(v)W(g(v)).

**BY** D1703's symmetrized Weyl operator differs from D1307's reference
Weyl operator by a scalar and hence belongs to the same full-scalar Pauli
group; membership in `C_2(A)` applies to every such Pauli.  Uniqueness uses
SP-WEYL's linearly independent Weyl basis.

<1>2. Conjugating the D1703 product for `W(v)W(w)` and comparing its unique
Weyl label proves `g(v+w)=g(v)+g(w)`.
**BY** D1703 and `<1>1`.

<1>3. Hence `g` is `F_p`-linear.
**BY** additivity on the elementary abelian `p`-group gives preservation of
integer multiples, which are exactly `F_p` scalars.

<1>4. The Weyl commutator is

    W(v)W(w)W(v)^*W(w)^*=psi_(F_p)(omega_n(v,w))1.

Conjugating it and using faithfulness of `psi_(F_p)` gives
`omega_n(gv,gw)=omega_n(v,w)`.
**BY** D1703, `<1>1`, and primitivity of `zeta_p`.

<1>5. Thus `g` is injective by nondegeneracy and therefore bijective in
finite dimension; it is symplectic.
**BY** `<1>3`--`<1>4` and D1701.

<1>6. Comparing scalar factors in the product calculation, now using
symplecticity of `g`, gives

    gamma(v+w)=gamma(v)gamma(w).

**BY** D1703's equal half-form multipliers on `v,w` and `gv,gw`.

<1>7. Every character of the additive group `V_n` has the form
`v |-> psi_(F_p)(ell(v))` for a unique `F_p`-linear functional `ell`.
**BY** choose a basis, identify each generator's `p`-th-root value through
the faithful character, and extend linearly.

<1>8. Nondegeneracy and invertibility of `g` give a unique `t in V_n` with

    gamma(v)=psi_(F_p)(omega_n(t,gv)).

**BY** the symplectic form identifies `V_n` with its linear dual; apply it
to `ell o g^(-1)`.

<1>9. For the graph relation of `(t,g)`, with origin `(0,t)` and direction
`{(v,gv)}`, D1715's equation is

    W(gv)T W(v)^*=psi_(F_p)(-omega_n(t,gv))T.

The operator `U` satisfies it by rearranging `<1>1` and `<1>8`.
**BY** D1702, D1715, and those steps.

<1>10. The graph line is one-dimensional, so
`E_p(Gamma_(t,g))=C U`.
**BY** `stabilizer-intertwiner-line.md` section 3 and `<1>9`.

<1>11. **QED** every unitary in the full D1307 second level, not merely a
named generating subset, lies in the functor image projectively.
**BY** `<1>1`--`<1>10`.

## 4. Fullness, faithfulness, and essential surjectivity

**PROVE** the line functor is an equivalence onto D1705.

<1>1. The computational preparation `delta_0` is the line of the state
`B_1={(0,b):b in F_p}`.
**BY** section 2 `<1>1` and the line theorem.

<1>2. Its adjoint is the line of the bare converse effect.
**BY** `stabilizer-functor-laws.md` section 5.

<1>3. Every nonzero scalar generator has the same D1705 class as the
identity relation on the zero object; the zero scalar is the image of the
empty `0->0` relation.
**BY** D1704--D1705 and `stabilizer-functor-laws.md` section 5 at rank zero.

<1>4. Every D1704 generating unitary is in the image by section 3, and the
remaining generators are in the image by `<1>1`--`<1>3`.
**BY** D1704 and those steps.

<1>5. Composition, tensor, and adjoint of generators remain in the image,
including products that become zero.
**BY** `stabilizer-functor-laws.md` sections 4--6.

<1>6. Induction on a D1704 generating expression proves that every arrow
class of `Stab_p^proj` is in the image.  Thus the functor is full.
**BY** D1704--D1705 and `<1>4`--`<1>5`; nonzero scalar rescalings disappear
and zero remains separate.

<1>7. If two source arrows have the same target class, either both classes
are zero, in which case both relations are empty, or they determine the
same nonzero intertwiner line.
**BY** D1715 and `stabilizer-intertwiner-line.md` section 3.

<1>8. In the second case, line recovery gives equality of the affine
relations.  Hence the functor is faithful.
**BY** `stabilizer-intertwiner-line.md` section 4.

<1>9. The object assignment sends the standard rank `n` object to
`H_(F_p,n)`, and these are exactly D1705's objects.  It is therefore
surjective on objects.
**BY** D1704--D1705.

<1>10. Fullness, faithfulness, and surjectivity on objects make the functor
an equivalence; `stabilizer-functor-laws.md` supplies its dagger symmetric monoidal
structure.
**BY** `<1>6`--`<1>9` and the standard definition of equivalence.

<1>11. **QED** the equivalence clauses.
**BY** `<1>10`.

## 5. Exact source and convention comparison

**PROVE** the construction has the same source/target scope as the cited
stabilizer theorem without importing its variance or dagger.

<1>1. SP-BC24 uses `omega_input-omega_output`, the global negative of
D1702's `-omega_input+omega_output`.  These forms have the same
perpendiculars and Lagrangian affine subsets, and both use ordinary
relational composition, diagonal identity, and direct-sum tensor.
**BY** SP-BC24 `section_AffLagRel.tex` 143--169 and scalar multiplication
of a form by `-1`.

<1>2. D1702's graph order is `(v,gv+t)` and its composition is covariant.
Section 3 `<1>9` checks directly that this graph maps to the SP-EGOROV
implementer; no CK21 row/right-action matrix is transposed into the
definition.
**BY** D1702 and section 3.

<1>3. In D1703 coordinates,

    W^s(a,b)=psi_(F_p)(a*b/2) Z(-b)X(a).

Thus CK21's boost/positive phase Pauli labels describe the same projective
Weyl operators after `(q,p)=(a,-b)`.
**BY** D1703's standard operator formula and SP-CK21 `lagrel.tex`
3585--3649.

<1>4. CK21's finite-centre Pauli normalizer and D1307's full-`U(1)` Pauli
normalizer coincide.  One inclusion is immediate.  For the other, section 3
shows a D1307 normalizer phase `gamma` is a character of the exponent-`p`
label group, hence has values in `mu_p`, already contained in CK21's finite
centre.  The induced `g` is bijective and conjugation fixes central scalars,
so this inclusion is equality rather than a proper endomorphism of the finite
Pauli group.
**BY** D1307, SP-CK21 `lagrel.tex` 3590--3649, and section 3
`<1>5`--`<1>6`.

<1>5. D1704 freely adjoins every actual complex scalar.  Passing to D1705
removes every nonzero such rescaling and leaves zero as a separate class,
exactly the scalar scope in SP-CK21.
**BY** D1704--D1705 and SP-CK21 `lagrel.tex` 3647--3654.

<1>6. SP-CK21 Theorem `theorem:spekkens`, lines 3703--3744, corroborates
the resulting symmetric monoidal equivalence.  The present proof establishes
it independently through the line, so the source's row/column presentation
does not create a hidden graph-composition convention.
**BY** sections 1--4 and the cited source statement.

<1>7. Dagger preservation comes solely from the adjoint calculation in
`stabilizer-functor-laws.md` section 5.  SP-BC24's dagger additionally applies
`(a,b)|->(a,-b)` to both endpoints and is not used.
**BY** the source audit, SP-BC24 `section_AffLagRel.tex` 169--177, and the
companion proof.

<1>8. The grouped tensor order is the exact D1702 reorder and admitted
SP-TENSOR comparison, not CK21's unconverted interleaved or row ordering.
**BY** `stabilizer-functor-laws.md` section 6 and the source audit.

<1>9. **QED** the source, row/column, graph, normalizer, scalar, dagger, and
tensor convention bridge.
**BY** `<1>1`--`<1>8`.

## 6. Exact refined conclusion

<1>1. Every nonempty relation has a one-dimensional, actual stabilizer
amplitude line, while the empty relation has only zero.
**BY** `stabilizer-intertwiner-line.md` and section 2.

<1>2. Sending the relation to `[T]` for any nonzero member of that line is
well-defined and gives a dagger symmetric monoidal functor.
**BY** one-dimensionality and `stabilizer-functor-laws.md`.

<1>3. The functor is full, faithful, and surjective on objects, including
all D1307 second-level unitaries.
**BY** sections 3--4.

<1>4. Its conventions and source reliance have exactly the canonical odd
prime, all-invertible-scalars scope.
**BY** section 5.

<1>5. No representative normalization, probability, CP map, compact
preservation, or arithmetic-source exhaustion has been asserted.
**BY** D1715's Scope and inspection of the proof.

<1>6. **QED** the refined canonical SP-STAB-REL statement, with SP-LREL and
SP-COMPACT retained as explicit admitted dependencies.
**BY** `<1>1`--`<1>5` and the canonical dependency discipline.
