# Composite relative quantum boundary — adjudication, 2026-09-09

One root prover pass supplied seven proposed definitions and six finite
claims. An independent verifier reconstructed the finite fields and graph
operators, and a separate target-blind hostile critic independently
recomputed the principal matrices and probabilities. Both used native
inherited runtimes; exact model identifiers were not independently exposed.
The same-family and coordination limitations are recorded in the retained
verdict `composite-boundary-r1.md`. No cross-family or machine-checked proof
claim is made.

The single hostile verdict was FAIL(CB-C1): no FATAL, one MAJOR scope error,
and two MINOR integration fixes. The verdict is retained unchanged. Root
applied one repair wave and verified each disposition mechanically, without
a new hostile round.

## Dispositions

- **CB-C1, closed.** Ordinary trace/determinant and the full randomized
  copied-mixture formulas still cover n>=1. The copied map C is now defined
  for n>=1 in D1602, with its actual arithmetic realization proved in the
  construction shard. The moving-sector conditional state is explicitly
  restricted to n>1 in the proof, claim row and labbook. At n=1 the moving
  projection and denominator are zero, and no conditional state is defined.
  Root independently executed the degree-one relational matrices over
  F2/F2, F3/F3 and F4/F4: R is identity, the code dimensions are 2,3,4,
  the full reference mass is one, and the moving weight is zero.
- **CB-C2, closed.** `S_d` denotes only the single d-cycle. `u_n^rel`
  denotes the all-period direct sum. Definitions, notation and the labbook
  use these distinct types; the primitive boundary uses S_n on C^n.
- **CB-C3, closed.** The independent verifier repaired the CLI without
  changing any mathematical gate body. The promoted standalone checker
  advertises all nineteen `--red-<name>` modes, and plain `--red` aliases
  the actual collective mutation. Root repeated the same help discovery
  used by session-close and invoked every discovered flag without an
  argument. All twenty returned exit one and JSON MathematicalFailure
  at the registered C1--C9 gate, not a parser/interpreter error.

The repaired checker SHA256 is
`5816197b901d74012112d2957c5df45ad7b634b426e6c64bb46c16ed0f3df3d1`.
Root repair evidence is `numerics/composite-boundary/results/ROOT-REPAIRS.json`.
The original proposal hashes and the independent frozen runs are adjacent.
The critic's original pre-CLI-repair hashes remain in its mutation audit;
they are not silently identified with the repaired executable.

Uniform randomization is explicitly source-accessible using n copies of
R^j/n for each j. This clarifies the critic's verified scalar-field point:
no sqrt(n) needs to be adjoined to Q(zeta_p,sqrt(p)).

## Admitted scope

Admit CMP-SOURCE, CMP-REL, CMP-BOUNDARY, CMP-NATURAL, CMP-FOURIER and
CMP-TRACE as PROVED at their repaired stated scopes, with D1601--D1607.

The substantive new object is an origin-free multiplication-graph quantum
register. Its invariant orbit sums are assembled from three actual field
registers. Simultaneous Frobenius is removed, while actual middle-register
Frobenius transported through multiplication shifts the relative index.
The generated common algebra contains a primitive M_n block for every
relative extension degree n>1, in every characteristic.

One copied POS reference and two fixed zero ancillas prepare that block
with success c_n(t^s)/(n t^(sn)), whose first coefficient is s phi(n)/n.
The same final coherent test gives conditional probabilities 1 after
relative Frobenius, 0 after identity, and 1/n after computational dephasing.
The full raw primitive branch vanishes at t=1. Its first nonzero grade
retains the positive conditional quantum system and the CP action.

All histories are retained in the prelimit instrument. The result neither
identifies the full single-field algebras across characteristic nor proves
that they collapse to scalars. Separate invariant descent is compared
orbitwise. Physical orbit multiplicities and some further Fourier outcomes
retain p. Boundary tensor/sum closure and a conventional C_1 are not required.

Fixed-base embeddings preserve the full period decomposition without
orbit origins. The smaller primitive degree stays the same degree in a
larger ambient code; no changing-base naturality or substitution of the
upper primitive cut is asserted. The Fourier theorem concerns only the
specified first-register return, with its phase and ambient failure.

The finite spectrum and determinant identities use the actual orbit
multiplicities. Channel spectrum and implementer spectrum are distinguished.
An infinite-degree completion, its trace/regularization, and any Riemann
spectral identification remain beyond these six admitted statements.

## Evidence and integration

The independent verifier passed 76,584 exact comparisons across six
relative fibres, including F16/F4 and characteristic-dividing degrees.
All nineteen distinct mathematical mutations failed; the final CLI has
twenty advertised flags because plain --red is an alias. The critic
independently checked all six fibres, the original nineteen mutants, and
two additional probability/phase data mutations. These finite computations
are falsifiers supporting the general structured proofs, not substitutes
for those proofs.

The two repaired shards, definitions, claim rows and owning labbook section
are integrated together. The labbook contains the explicit F4 graph vectors
and its 1/4, 0, 1/8 arithmetic joint probabilities. Repository-wide checks
and the final PDF inventory are recorded separately in the result directory.

PASS after one repair wave. No open MAJOR remains on the six admitted scopes.
