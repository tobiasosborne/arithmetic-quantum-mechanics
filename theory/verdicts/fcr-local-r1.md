# FCR-1 hostile verdict — round 1

- Target: `theory/lanes/fcr1/prove/fcr-local.md` and its proposed `PATCH.md`.
- Prover/critic limitation: same-family prover and critic (`gpt-5.6-sol`, reasoning `xhigh`, `codex exec`).
- Lane: blind critic lane. I read the artifact, trunk single sources, target brief, registered source bodies, and checker only; I did not read the prover's `SUMMARY.md`, `LAST-MESSAGE.md`, `WORK-ORDER.md`, or other lane material.
- Prior verdicts on this target: none found.
- Verdict: **FAIL**. The local-ring theorem cluster survives, but `FCR-REG` is stronger than the statement-level comparison proves.

## Numbered objections

### OBJ-1 — MAJOR — `FCR-REG` is not an exact claim-by-claim regression

**(a) Exact location.** `fcr-local.md` §9 table row `FCR-CHOICE`, especially `<1>1`–`<1>2`; `PATCH.md` proposed rows `FCR-CHOICE` and `FCR-REG`; compare trunk `claims/CLAIMS.md` rows `WH-CHOICE` and `WH-FORM`.

**(b) Independent computation/counterexample.** Set `R=κ=F_3`. The proposed `FCR-CHOICE` row and §8 `<1>3` concern the torsor of **all bare-algebra isomorphisms**. Since the algebra is `M_3(C)`, its target automorphism group is `PGL_3(C)`, an infinite group (for example, the classes of `diag(λ,1,1)`, `λ∈C^×`, already give infinitely many automorphisms). Trunk `WH-CHOICE`, by contrast, concerns **frame-preserving** isomorphisms and asserts a torsor of order
`q²|SL_2(κ)|=9·24=216`. These are not the same torsor or the same statement, even at the first odd field. Likewise, the table maps `FCR-RAD` to `WH-FORM`, but `WH-FORM` also asserts the cocycle and two automorphism-group clauses that `FCR-RAD` neither states nor proves. Gate G10 checks `F_3` counts, matrices, rank, commutant, lines, and the group profile; it does not compare the text or hypotheses of these rows. Thus “specialize exactly, claim by claim, to the `WH-*` rows” is false as written.

**FIX DEMAND.** Replace `FCR-REG` by a clause-level compatibility statement that lists exactly the recovered clauses and expressly excludes the frame-preserving torsor and unproved `WH-FORM` clauses, or add the missing frame/symmetry analysis and a genuine statement-level fixture; do not retain `PROVED` on the current wording.

**SURVIVING WEAKER STATEMENT.** At odd fields, D12–D16 and the ring calculations recover on the nose the character torsor, form/radical calculation, Weyl relation, central-simple algebra, Stone–von Neumann clauses, Lagrangian/model counts, and odd-`β` transport listed in §9; they do not recover every clause of every named trunk row.

### OBJ-2 — MINOR — the Wood-to-D12 socle identification is asserted, not derived

**(a) Exact location.** `fcr-local.md`, `FCR-GEN <1>1.<2>2`–`<2>3`; source locator `refs/wood-ajm-1999/wood_duality_ajm121_1999.txt:107-147`.

**(b) Independent computation/counterexample.** Wood defines `S(R)` as the submodule generated by irreducible submodules. D12 separately defines `soc(R)=Ann(𝔪)`. The shard silently writes Wood's socle as the D12 object. The equality is true, but it needs a leaf: every simple ideal `J` satisfies `𝔪J=0` by nilpotence/minimality, hence lies in `Ann(𝔪)`; conversely, for every nonzero `x∈Ann(𝔪)`, the cyclic ideal `Rx=κx` is one-dimensional over `κ` and simple, so every such `x` lies in Wood's module socle. Only after this equality is established does Wood's one-summand condition reduce to `κ≅Ann(𝔪)`. The source itself does not make this D12 identification.

**FIX DEMAND.** Insert the two inclusions `S(_RR)=Ann(𝔪)` as a Lamport substep before `FCR-GEN <1>1.<2>3`.

**SURVIVING WEAKER STATEMENT.** With that elementary identification supplied, the permutation and multiplicity clauses are vacuous for a commutative local ring and Wood's Frobenius condition is exactly `κ≅soc(R)`.

### OBJ-3 — MINOR — several proposed claim rows are not closed under their intended quantifiers

**(a) Exact location.** `PATCH.md` rows `FCR-COMM`, `FCR-SVN`, `FCR-POL`, and `FCR-BETA-ODD`; compare the ASSUME headers of `fcr-local.md` §§3, 4, 6, and 7.

**(b) Independent computation/counterexample.** The proof of `FCR-COMM` quantifies over a finite local `R`, every nontrivial `ψ∈X(R)`, and every `β∈Adm(ω)`, but its row binds only `β`, leaving `R` and `ψ` free. The `FCR-SVN` and `FCR-POL` rows use `H_β` and `A_{ψ,β}` without saying “for every `β∈Adm(ω)`”; `FCR-BETA-ODD` uses `H_β→H_{β+s}` without binding the starting `β`. These are not counterexamples to the intended mathematics—the shard proves the universal versions—but a DAG statement must be a closed assertion for its quantifier audit to be meaningful.

**FIX DEMAND.** Bind `R`, `ψ`, and `β` explicitly in each affected claim row, matching the corresponding ASSUME header.

**SURVIVING WEAKER STATEMENT.** For every D12 finite local ring and the `ψ,β` quantified in the shard headers, the displayed conclusions hold at exactly the scopes proved below.

### OBJ-4 — MINOR — checker documentation redefines trunk `q`

**(a) Exact location.** `theory/checks/fcr_local_EXPECTATIONS.md:5-8` and its tables, against proposed D12 and the proposed `notation.md` row for `q`.

**(b) Independent computation/counterexample.** D12 fixes `q=|κ|`. For `R=Z/9`, this is `q=3`, while the expectations document declares `q=|R|` and labels the seed with `q=9`; for `F_3[t]/(t^3)`, D12 again has `q=3` while the checker document uses `q=27`. The numerical computations use `|R|` correctly, but the symbol collision violates the single-source notation and makes statements such as “`q³` members” ambiguous between `27` and `19683`.

**FIX DEMAND.** Rename the checker/document variable to `n=|R|` (or write `|R|` throughout) and reserve `q` for `|κ|`.

**SURVIVING WEAKER STATEMENT.** Every recorded checker count remains correct after this purely notational relabelling.

### OBJ-5 — MINOR — G6 and G11 are honest diagnostics, but not mutation-backed gates

**(a) Exact location.** `theory/checks/fcr_local_RED-MATRIX.md:9-21,40-50`; `fcr_local_check.py` functions `gate_G6` and `gate_G11`; proposed `PATCH.md` row `FCR-ALG`, tested-in `(G5,G6)`.

**(b) Independent computation/counterexample.** Green exited `0`. All seven registered red modes exited `1` at their pre-registered first gates: nongenerating→G5; transpose→G2 (then G4/G10); Frobenius-blind→G1 (then G3); free-only→G7; torsor→G8; dim→G5; half-Weyl-drop→G9. No registered mode reaches G6 or G11; the red matrix itself calls both decoration. On checker copies in this critic lane, changing G6's designated character from `ψ_3` to `ψ_1` made G6 fail, and deleting the identity datum from the profile census made G11 fail at all seven seeds; changing one literal `F3_MODEL` phase made G10 fail. Thus the assertions can fail, but G6 and G11 lack callable red paths in the shipped checker and cannot count as L1 mutation evidence yet.

**FIX DEMAND.** Implement the already specified `--red-g6-arena-confusion` and `--red-profile-drop-identity` modes (and add them to `MODES`, `TARGET`, and the reachability matrix), or remove G6 from the claim row's tested-in cell and label G11 as diagnostic data only.

**SURVIVING WEAKER STATEMENT.** G1–G5 and G7–G10 have specific registered mutations; unmutated G6 and G11 remain useful exact diagnostics, not falsifiers.

## VERIFIED CORRECT — repair lane: do not churn this fenced section

<!-- BEGIN VERIFIED CORRECT -->

1. **Registered sources and exact locators.** The Stacks passage at `algebra.tex:12739-12756` states and proves nilpotence of the Jacobson radical of an Artinian ring. Wood's `:107-147` gives the principal decomposition, top, module socle, QF condition, and Frobenius multiplicity clause; `:492-504` is Theorem 3.10; `:556-562` defines generating characters; `:570-581` is precisely the no-nonzero-ideal-in-the-kernel criterion. The shard re-derives the local reduction rather than citing a field port; only OBJ-2's equality leaf is missing.

2. **`L-ANN`, recomputed in both orders.** For `Z/9`, `I=(3)=Ann(I)=Ann(Ann(I))`. With `ψ(r)=ζ_9^r`, the raw exponent census for `Σ_{r,i}ψ(ri)` is `{0:15,3:6,6:6}`, which reduces by `1+ζ_9^3+ζ_9^6=0` to `9`; summing by `r` gives three annihilator elements, each contributing `|I|=3`, again `9`. For `F_3[t]/(t^3)`, take `I=(t)` of size `9`, `Ann(I)=(t²)` of size `3`, and `Ann(Ann(I))=I`. With the top-coefficient character, the raw exponent census is `{0:99,1:72,2:72}`, reducing by `1+ζ_3+ζ_3²=0` to `27`; the other order has three annihilator elements each contributing `9`. There is no hidden nontriviality assumption in either character-sum step.

3. **Generating characters.** After the OBJ-2 leaf is inserted, `FCR-GEN` is correct: `Gen(R)≠∅` iff `dim_κsoc(R)=1`; for a fixed generating `ψ`, `ψ_u` is generating exactly for units, and the character-module isomorphism gives free transitivity. The non-Frobenius probe census `2+24=26` and its displayed kernel ideals are correct.

4. **Characteristic/quantifier audit.** `FCR-GEN`, `FCR-RAD`, and `FCR-COMM` use no half and are proved in every residue characteristic. An independent `Z/4` census gave `I_{ψ_1}=0`, `rad=0`, while `I_{ψ_2}=(2)` and `rad=(2)²` of size `4`; `β₀-β₀^T=ω` and the commutator identity still hold, while `2` is not a unit. `FCR-BETA-ODD` genuinely uses `2∈R^×`. In `FCR-ALG`, simplicity, scalar centre, and dimension `|R|²` are proved without `2`; the displayed transport from arbitrary `β` to the reference matrix model uses `1/2`, so scoping the whole conjunction by `2∈R^×` is honest but weaker than the first three leaves. `FCR-SVN`, the character/model part of `FCR-POL`, and the `β`-immateriality part of `FCR-CHOICE` use that odd theorem. The local unit criterion proves `2∈R^×` iff its residue is nonzero, hence iff the residue characteristic is odd.

5. **Algebra and the G6 arena ruling.** `FCR-ALG <1>1`–`<1>4` concern the **formal twisted algebra** `A`, so G6's `|I_ψ|²` must mean its centre/commutant in that arena. For `Z/9`, `ψ_3`, `I=(3)`: the formal algebra has dimension `81` and centre dimension `|I|²=9`; the `9`-dimensional Schrödinger representation is non-faithful, its image algebra has dimension `|R|·|R/I|=9·3=27`, and the commutant of that matrix image is `span{X(0),X(3),X(6)}`, dimension `|I|=3`. The checker records all three numbers correctly. No sentence of `FCR-ALG` is false once “formal” is kept fixed; the nongenerating G5 mutation properly measures the matrix image.

6. **Polarizations, including the non-free model.** For `L=(3)⊕(3)⊂(Z/9)²`, exact enumeration gives `|L|=9`, `L^{⊥_ψ}=L`, `3L=0`, and `β₀(L,L)=0`. Thus `A_L≅C[(Z/3)²]`; `|V/L|=9`, the Weyl coset basis makes `A` right-free of rank `9` over `A_L`, and every induced model has dimension `9`. The formula `χ₀(W(l))=ψ(-β(l,l)/2)` has the correct sign because
`ψ(β(l,l'))χ₀(W(l+l'))=ψ(-β(l,l)/2-β(l',l')/2)`. The argument does not require `L` to be free. The general non-free witness `soc(R)⊕𝔪` and its Nakayama-quotient argument are correct.

7. **Stone–von Neumann and canonicity.** The columns proof gives every simple `M_{|R|}(C)`-module as the standard column module; the eigenvalue proof of Schur is valid over `C`; polar decomposition/rescaling yields uniqueness up to `U(1)`; twisting a simple module proves innerness. `FCR-CHOICE <1>3` proves exactly the proposed weaker wording “no automorphism-invariant point” for the bare-isomorphism torsor and does not claim a stronger naturality theorem. The only choices in the construction are the named `ψ,β`; `(L,χ)` labels a realization, coset representatives occur only in a dimension proof, and the matrix ordering/basis dependence is named.

8. **Checker specificity.** Besides the green run, the registered red modes have distinct fingerprints and the driver continues far enough to show the declared secondary firings. Symbolic inspection found no `0=0` acceptance gate: G2 compares independently formed tables; G3 compares an enumerated radical with `I²`; G4 multiplies monomial matrices; G5 uses exact phase Gram matrices and a potential-equation commutant; G7 enumerates submodules by closure; G9 compares the quadratic coboundary with the bilinear table. Custom data mutations killed G10, G6, and G11 as recorded in OBJ-5.

9. **Reliance and scope.** No proof step relies on the REFUTED `WH-FUNCT-b-SEC` row, an unregistered source, or `v0.1/`. The proposed nine claim rows contain none of the forbidden out-of-scope terms. The 449-line shard respects the 200–500 line cap.

<!-- END VERIFIED CORRECT -->

## Status-register check

`FCR-GEN`, `FCR-RAD`, `FCR-COMM`, `FCR-BETA-ODD`, `FCR-ALG`, `FCR-SVN`, `FCR-POL`, and `FCR-CHOICE` are in the same substantive proof register as their nearest trunk PROVED clauses; OBJ-2–OBJ-5 are local source/row/checker hygiene repairs and do not furnish counterexamples to those eight mathematical statements. `FCR-REG`, however, is not entitled to `PROVED` at its present strength: OBJ-1 is an open MAJOR on the proposed statement. It must be weakened to clause-level compatibility or remain at most `SKETCH` until the missing frame/symmetry comparisons are proved and tested.

FAIL(OBJ-1)
