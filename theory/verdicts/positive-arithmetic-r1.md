# Positive arithmetic reference and finite operational boundary — hostile verdict

2026-09-09. **Same-family prover/critic, target-blind lane.**
Native inherited Codex runtime; no model override, nested CLI or subagent.
A fresh thread was unavailable. This context contains earlier unrelated
Galois and mixed-chart reviews, not the new positive prover's reasoning.
The target is the frozen positive-arithmetic package, public goal/scope
briefs, admitted definitions/claims and independent verifier. Prior chart
familiarity is a limitation, explicitly recorded rather than called a
cross-family check. Root confirmed both final proof and verifier freezes.

**Mathematical decision: PASS, one MINOR wording repair, no FATAL/MAJOR.**
The six positive statements survive with their stated finite-field,
fixed-process and retained-characteristic scopes. This is also substantive
evidence for the clarified minimum of a useful nontrivial boundary.
It does **not** prove the stronger MIX-ALL or identify the old angle family.
Goal completion and registry promotion remain root's adjudication actions.

## 1. MINOR — the positive Taylor coefficients belong to D, not rho

**(a) Location.** `prove/CLAIMS-PROPOSED.md`, POS-STATE row (line 7),
`Its entire expansion has the positive D1502 coefficients` immediately
after calling the reference a state. Compare `reference-and-protocol.md`,
section 1 `<1>3.<2>2--<2>3`, section 2 `<1>1.<2>1`, and the labbook's
explicit D_E expansion in its first proposition (lines 94--101).

**(b) Independent computation.** The normalized reference expands as
`rho_E(h)=P0+h(B_E-rP0)+O(h²)`. Its derivative has expectation −r on
the zero vector, so that derivative is not positive for any allowed r.
It is D_E whose ordinary Taylor coefficients are positive. The normalized
rho_E is a positive state at real h>=0 and an exact Poisson mixture; the
factor exp(−rh) must remain part of that mixture, or of its scalar process
normalizer. Both proof and labbook already use those correct formulas.

**(c) FIX DEMAND.** Replace the ambiguous sentence by an explicit statement
that `D_E(h)=sum h^k A_(E,k)` has positive coefficients, and explicitly
name rho_E as the normalized Poisson mixture in the next sentence.

**(d) SURVIVING STATEMENT.** All state positivity, uniqueness, positive
unnormalized coefficients, Poisson grades, code restrictions, series
processes and finite-profile conclusions remain unchanged. No proof
repair or new hypothesis is required.

## Independently VERIFIED CORRECT — protected mathematical region

Do not churn the following verified derivations during the wording repair.

1. **State, prescribed class uniformity and arithmetic comparison.**
   The zero and nonzero exact-period projectors partition the field basis.
   Their ranks are 1 and b_d(p). Dividing each prescribed mass by its
   rank uniquely fixes the density within the stipulated real span.
   The divisor identity gives Tr D_E=e^(rh), and every period weight
   is positive for h>0. At h=log p every eigenvalue of D_E is one.
   The zero/one commutator has expectation
   `e^(-rh)[1-(e^h-1)/(p-1)]`, proving that the full-algebra state is
   tracial exactly at h=log p. No traciality is used elsewhere.
2. **Positive coefficients and Poisson tensor.** Expanding the finitely
   many exponentials gives J_k(d), including J_k(1)=1, and the product
   over distinct prime divisors proves strict positivity. Reversing
   divisor sums gives sum_(d|r) J_k(d)=r^k. Consequently A_Ek has trace
   r^k/k! and is faithful on the nonzero subspace for k>=1. Normalizing
   gives actual states. Cauchy tensor multiplication of these coefficients
   gives the multinomial formula, with convergence controlled in trace
   norm by e^(R|h|). Normalized state coefficients themselves need not
   be positive, as objection 1 stresses.
3. **Every named embedding and tower, including p|degree.** Injectivity
   makes i(x)^(p^d)=i(x) equivalent to x^(p^d)=x. Hence each retained
   coefficient restricts literally through J_i, independent of ambient
   degree, and J_i^*D_EJ_i=D_K and J_i^*L_EJ_i=L_K. Normalized exact
   success is e^((s-r)h); finite-profile success is (1+sh)/(1+rh).
   Their leading failure coefficient is r-s for a proper embedding.
   All tower restrictions compose by the actual J equations, using no
   inverse degree. Fourier sends the reference to F_E D_E F_E^*, and
   the actual FJ=VF_K identity carries both success and ambient failure
   branches to the corresponding V decoder. This is transported context,
   not reference invariance or a reset.
4. **Actual moments and active arithmetic.** For a diagonal state times
   a permutation, only fixed basis labels contribute to its trace. This
   gives the Frobenius moment e^((gcd(r,k)-r)h), for negative, zero and
   positive k. A multiplication gate fixes precisely the tuples with a
   zero control; independent inputs therefore give active mass
   `(1-e^(-rh))^d` and order d with leading coefficient r^d. Expanding
   2I-M-M^* gives twice this mass without requiring translation-invariance
   of the target reference. Conditioning moving labels cancels actual
   orbit multiplicities and recovers exactly the stated FRL block weights.
5. **Positive-series category.** The Choi matrix of each coefficient is
   positive and has trace bounded by d z_k on an input block M_d. This
   supplies entrywise and norm bounds by a convergent scalar majorant on
   each compact interval. The limits are CP at each finite amplification;
   division by Z(h)>=z_0>0 preserves positivity and the ordinary trace
   bound. Cauchy composition and tensor satisfy the coefficient bounds,
   including on entangled inputs. Cross multiplication is an equivalence
   relation because a scalar series with nonzero constant term cancels
   coefficientwise; the relation respects composition and tensor.
   Normalization descends by taking traces and cancelling that scalar.
   Constant actual TNI arithmetic maps and the two explicit reference
   preparations are therefore valid arrows. Their scalar normalizers
   are essential; a positive trace alone is not being used to certify CP.
6. **The finite-profile lemma is valid for arbitrary fixed CP maps.**
   On each zero/nonzero input mask, B_S is faithful and the exact D word
   begins at h^|S| B_S. If a positive map kills B_S, the inequality
   `0<=C<=c B_S` kills every positive matrix C supported on that mask;
   linearity then kills its entire corner. Therefore no higher coefficient
   from a killed mask can reappear. The first surviving mask size v
   contributes exactly the same positive matrix sum in D and L. At least
   one mask survives for a nonzero CP map because sum B_S is faithful
   on the full input. This proves v<=m with no finite-dimensional sample
   bound and no restriction to diagonal, separable or product outputs.
7. **Later conditioning and finite protocols.** Any later fixed CP map,
   including one entangling the output with fresh independent references,
   is applied by composition to the original word. This preserves the
   preceding argument, counting all allocated references. Preallocating
   finitely many adaptive references and discarding unused ones gives
   each named branch a fixed CP map. Exact and finite normalizers both
   have constant term one, so their leading event coefficients and
   conditional densities agree. Prefix/final conditional probabilities
   use the ratio of their respective leading traces. Leading states
   alone are correctly rejected: a later nonzero-label test kills P0
   but finds the hB_E branch in the retained profile. No nonlinear
   conditioning operation is asserted to be a linear categorical arrow.
8. **Uniform mixed protocol.** For a0 outside K, either (a0,a0^q) is
   K-independent or a0^q=lambda a0 with lambda!=1, in which case
   a=a0+1 makes (a,a^q) independent. Trace-pairing nondegeneracy then
   makes b->(T(ab),T(a^q b)) onto K², proving the required controls
   exist for every proper extension, including all singular degrees.
   The selected nonzero controls cost order two and coefficient w_a w_b.
   Translation of the coherent trace fibre changes V|0> to V|1>, and
   Fourier plus retained J decoding and F_K^* gives target one with
   success one. Omitting relative Frobenius or multiplication gives zero.
   Omitting target Fourier uses the exact two Gram strata: success
   1/kappa if p∤degree, zero if p|degree. Dephasing the fibre gives
   success 1/kappa because each trace-one basis label has the same
   compressed Fourier vector, with coherent enhancement removed.

## Independent recomputations and necessity tests

`probes/independent.py` imports no target prover or checker implementation.
It reuses only the critic's earlier unrelated finite-field arithmetic helper.
Relative traces are recomputed as traces of multiplication matrices over a
found K-basis, independently of the verifier's Frobenius-sum implementation.
Fourier coefficients are exact cyclotomic numbers, now also at p=7.

The reference probe checks positive coefficients through grade seven,
arithmetic comparison and all Frobenius powers from −r through r for
12 fields. This includes F64, so the divisor-six interaction is tested,
as well as p=2,3,5,7. A separate primitive-residue-tuple census checks
the numerical Jordan coefficients for d<=6,k<=3 against the divisor sum.
None of these finite probes is used as a proof of the general formulas.

The CP probe uses dense two-Kraus maps on the same-characteristic words
F4,F2,F2 and F9,F3, with all possible first surviving mask sizes. It
compares the first **matrix**, not only its trace. A rational Householder
unitary coherently mixes all input masks; later conjugated projections
kill its entire grade-zero image and select later orders. The exact and
finite outputs still have the same leading matrix at each tested order.
The independent red discards all total degrees above one and fails at
the dense two-Kraus matrix comparison; the unmodified probe passes.

Faithfulness is a necessary hypothesis, not a decorative adjective:
in a three-label test let B=P1 on the declared nonzero sector span(1,2),
and let a higher coefficient be P2. Projection onto label 2 then sees
an order-two branch in `P0+hP1+h²P2` and no branch in `P0+hP1`.
This counterexample violates precisely the faithful-mask premise used in
the proof, and explains why the actual strictly positive B_E matters.

The independently computed mixed protocols give:

| Embedding | Named twist | Full target / success | Without F success | Dephased success |
|---|---|---|---|---|
| F2->F4 | none | 1 / 1 | 0 | 1/2 |
| F2->F8 | none | 1 / 1 | 1/4 | 1/4 |
| F4->F16 | Frobenius twist | 1 / 1 | 0 | 1/4 |
| F3->F9 | none | 1 / 1 | 1/3 | 1/3 |
| F3->F27 | none | 1 / 1 | 0 | 1/9 |
| F5->F25 | none | 1 / 1 | 1/5 | 1/5 |
| F7->F49 | none | 1 / 1 | 1/7 | 1/7 |

Every row also gives target zero when U or M is omitted. The Fourier
calculation acts on the entire trace-fibre vector, not on a replaced
classical mixture. The frozen verifier independently supplies the binary
table-entry mutation and the coherent control/leave-and-return tests.

## Verifier audit and mutation reachability

Final frozen checker SHA256:
`272148e1bf157e15d682408b92fe8eeaf43d4849b027ab64b70eaea6c5e03718`.
The critic ran all **16** named red modes as actual separate CLI calls,
then an actual green CLI. Green exits zero with P1=779, P2=112, P3=11,
P4=104, P5=52, P6=64, P7=5 accepted assertions. The following reds all
exit one at mathematical assertions, not construction exceptions:

| Mutation | Gate | Killing assertion |
|---|---|---|
| mass | P1 | state normalization and positivity |
| jordan | P1 | independent exponential derivative versus Jordan coefficient |
| moment | P1 | all Frobenius power moments |
| embedding | P2 | named inclusion density restriction |
| fourier-invariance | P2 | Fourier transported code reference |
| tracial-complement | P3 | new reference positive on actual complement |
| compression | P4 | full return retains intermediate sector leakage |
| coherence | P4 | coherent fibre increases decoder success |
| fourier-phase | P4 | full mixed protocol output |
| profile | P5 | finite-profile first nonzero matrix coefficient |
| total-first-order | P5 | finite-profile first nonzero matrix coefficient |
| discard-later-rare | P5 | later branch survives the full profile |
| normalizer | P5 | scalar normalizers multiply coefficientwise |
| multiplication-entry | P6 | actual multiplication-table entry is observable |
| arity | P6 | actual active control mass and arity |
| erase-characteristic | P7 | Fourier-zero Born observable retains characteristic |

Every P1--P7 group has a reachable mutation. The two first-profile modes
reach the same assertion with different defects: one changes a grade-one
coefficient, while the other deletes later word grades. They are not
duplicate alterations of the same mathematical data. The multiplication
mutation changes a separate gate table and leaves field/trace/Fourier
construction intact, so its exit is an observable failure.

Symbolic simplification of the gates found no aggregate acceptance test
that merely compares an expression with itself. Period census comes from
actual field action; coefficient data come from exponent expansion and
are compared with Jordan products. The Cauchy word helper is checked
against an independent product polynomial in t. The profile matrices
omit genuinely different higher coefficients, while their first image
is tested under specified CP maps. Process normalization is checked on
actual matrix-unit superoperators. Some sample identities at t=1 have
zero moving mass and are individually vacuous; positive-t and separate
coefficient tests carry those assertions. Positivity of arbitrary process
amplifications comes from the written proof, not from finite trace tests.

Three additional data mutations on temporary verifier copies were run:

- Redistributing finite-profile B weights by +1/4 and −1/4, preserving
  positivity and total trace, dies at P5's first-matrix comparison (#5).
- Replacing an actual copied output by the independent reference dies
  at the copied-versus-independent event-order assertion (#37).
- Changing the positive scalar-lifted numerator's quadratic coefficient
  dies at cross-multiplied representative equality (#51).

These are distinct late data/semantic tests, not blanket failures at P1.
All temporary copies were removed. Exact commands, exits and diagnostics
are frozen under `probes/`; target verifier and source artifacts were not
modified. The independent probe's initial implementation error in a
cyclotomic division was corrected locally before its final green; it was
not reported as a mathematical objection or as target mutation evidence.

## What is a limit here, and what is not

**Substantive surviving boundary.** At fixed actual p and named field
diagram, t=e^h tends to one while the added references vary. Unconditionally
rho_E tends to the zero ket. The nontrivial information is in positive
finite-order event records and their retained polynomial, whose sufficiency
under every fixed CP continuation is proved. It is not inferred merely
from keeping a constant matrix algebra. The all-proper-extension protocol
has an order-two positive coefficient and a uniform zero-versus-one
arithmetic distinction, with a separate coherence-sensitive success test.
Independent nonzero controls retain their order d, code failure retains
coefficient r-s, and the moving-orbit reference recovers the admitted
positive matrix system. These meet the clarified minimum at the stated
level of arithmetic reference experiments.

**Finite profile is a different finite-h family.** Already for r=2 the
zero-label weight of rho has expansion `1-2h+2h²+...`, whereas the
finite lambda profile gives `1-2h+4h²+...`. The profile preserves leading
branch records under fixed continuations, not the entire normalized
family or its arithmetic comparison at h=log p. The degree bound counts
all references allocated to a finite protocol; adding new independent
references requires tensoring their profiles, not pretending the old
degree bound never grows. Arbitrary h-dependent cancellation/angle maps
are outside POS-FINITE even though the broader series category contains
some additional h-dependent maps.

**Copied and independent references remain distinct.** For independent
E,E, the event x=y!=0 has order two and coefficient sum_x w_x². Copying
one E reference through its actual isometry gives order one and coefficient
r. Likewise a field-coordinate unitary transports a correlated state and
does not reset it to independently prepared prime-field references.
The proofs and labbook explicitly retain these distinctions. A prescribed
intermediate measurement is likewise a different process from coherent
evolution with no such measurement; retained tags do not undo dephasing.

**Residual p dependence is observable.** For r=2 the nonzero boundary
density B_E/2 has purity 3/8 at p=2 and 1/6 at p=3, with dimensions four
and nine. Its Frobenius first moment is nevertheless 1/2 in both cases.
The zero-to-zero Fourier Born probability is 1/p^r. Field tables, phases,
individual label weights and the protocol's preparation coefficient remain
named arithmetic data. No intrinsic characteristic-one category or limit
of varying Hilbert dimensions has been proved.

**No promotion of old MIX-ALL.** The old chart continuation changes the
Gram angle matrices; this construction keeps those matrices fixed. In
particular its arithmetic overlap remains fixed while the earlier c(t)
coalesces. The new reference does not automatically reproduce the old
conditional tracial chart normalization, weighted angle connector, or
all its prescribed mixed continuation data. The package and public scope
contract expressly say so. Mathematical PASS here is not a verdict on
that stronger conjecture, which must remain CONJECTURE.

## Quantifiers, choices, reliance and register lockstep

All new arithmetic statements concern every actual finite field and all
named embeddings in the stated finite diagrams. Singular relative degrees
are included wherever claimed, and the trace-pair construction has no
characteristic restriction. The CP/profile theorem is universal over
fixed finite-dimensional CP maps and finite reference protocols. No
finite checker bound is substituted for any of these quantifiers.

The actual p, named primitive root, field diagram, embeddings, factor
order, positive square roots and selected protocol controls a,b are
explicit data. Period references require no orbit-origin choice. The
comparison with the marked FRL observable algebra retains its existing
marks. Reference restriction through J is the stated code restriction,
not a claimed equality of forward-encoded rho_K with ambient rho_E.

Dependencies FRL-ORBIT/POS, FRB-TRACE/TRANSFER, MIX-GRAM and FRP-CAT/CP
are admitted PROVED rows; no REFUTED claim, unregistered external theorem
or deprecated snapshot enters the new argument. Remaining ingredients
are displayed divisor sums, finite-matrix positivity and convergent
series computations. The six claim rows, both proof headers, six labbook
status macros and prover summary all remain SKETCH pending adjudication.
Apart from objection 1's coefficient wording, they agree in mathematical
strength. The new assertions do not change MIX-ALL's status.

The structured shards have 296 and 327 lines. The final labbook independently
compiles to seven pages with the shared preamble, pdflatex exit zero and
no overfull boxes. It states the definitions, traces, exceptional scopes
and retained characteristic dependence. Final file hashes and build
evidence are in `probes/artifact-audit.json`; root still owns integration,
the full lockstep gate and mechanical repair adjudication.

PASS
