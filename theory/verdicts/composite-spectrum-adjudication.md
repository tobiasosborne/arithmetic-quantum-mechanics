# Degree completion and positive trace — adjudication, 2026-09-09

This is the capped review of a new artifact following the finite composite
boundary, not another review of its repairs. One root prover pass supplied
D1611--D1612 and CMP-COMPLETE/MELLIN. A separate verifier implemented exact
finite falsifiers, and a fresh target-blind critic recomputed the finite
comparisons and independently checked the infinite derivations.
Native inherited runtimes and their same-family limits are recorded in
`composite-spectrum-r1.md`; no independently attested CLI model is claimed.

The hostile verdict is PASS, with no FATAL/MAJOR and two MINOR fixes.
Root applied one mechanical repair wave:

- CS-C1: the tail cutoff is now explicitly an INTEGER D>=1 in the proof,
  claim row and labbook. The critic's real-cutoff example D=29/10,
  beta=10 violates the unrestricted formula, whereas the integer-cutoff
  integral estimate is exactly the one proved. Density convergence,
  normalization, outerness and the zeta quotient are unchanged.
- CS-C2: `--red-central-dynamics` is explicitly an alias of the identity
  mutation in the final help interface and coverage record. There are
  thirteen distinct data changes, fourteen named flags, and fifteen
  advertised flags including plain --red. No claim of fourteen distinct
  changes remains. The root CLI edit preserves all mathematical function
  and class ASTs; source hashes distinguish the submitted and final copies.

Admit CMP-COMPLETE and CMP-MELLIN as PROVED in their stated scopes.
The nonunital divisor-corner maps are induced by the actual fixed-base
arithmetic encoders, and the weights phi(d)/d are the derived copied-reference
first coefficients. Their norm completion is the c0 sum of common matrix
blocks, with a densely defined faithful lower-semicontinuous semifinite
trace on that NONUNITAL algebra.

Relative Frobenius is a unital CP automorphism of the specified minimal
unitization. It is outer there, while remaining spatially implemented on
the named Hilbert space and inner in the larger product multiplier algebra.
This completion-dependent distinction is explicit, not a contradiction.
The implementing spectrum is the unit circle with root-of-unity point
spectrum; it is not claimed to be a Riemann-zero spectrum.

The additional central degree regulator gives a faithful invariant state
for every real beta>1. Its normalizer is
zeta(beta)/zeta(beta+1)-1, derived by positive divisor convolution in the
convergent domain. The ordinary trace-class density, the unbounded reference
trace and the implementing/channel spectra remain distinct. The regulator
is a specified choice, not a uniquely selected arithmetic time evolution.

The independent verifier passed 41,216 exact comparisons, including fields
through F4096, all twenty-two embedding choices, thirty-eight tower paths,
finite positive cutoff states, exact Dirichlet coefficients and explicit
cycle moments. The critic supplied its own finite-field computations and
three additional data mutations. Infinite statements are admitted on their
written and independently reviewed derivations, never from the finite bounds.

Frozen evidence is in `numerics/composite-spectrum/results/`, including
the submitted hashes, the canonical executable's repair verification and
coverage limits. The original verdict remains unchanged. No fresh hostile
review was used for the two minor repairs.

The subsequent joined limit of finite degree preparations is a separate
artifact and is not admitted by this verdict.

PASS after the specified minor repairs.
