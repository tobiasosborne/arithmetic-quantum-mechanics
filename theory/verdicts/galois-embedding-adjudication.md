# General Galois embedding registers — adjudication, 2026-09-09

One prover pass supplied D1401--D1405, GAL-FUNCTOR/DESCENT/IMAGE,
two structured shards and the self-contained labbook section. The
target-blind hostile pass in `galois-embedding-r1.md` returned PASS with
no repair demands. Its explicitly recorded runtime limitation is reuse of
an unrelated earlier reviewer context, containing no Galois prover reasoning;
no cross-family independence is claimed.

The critic independently checked the all-field argument, constant-rank
normalization/tensor closure, all allowed towers, Galois-equivariant CP
decoders and retained histories, normal core/normal closure, profinite
inverse limit and primitive finite-field comparison. It ran the frozen
verifier's green and 22 distinct mutation modes and recomputed additional
nonabelian and cyclic examples. Exact sources are registered locally.

Root admits all three claims as PROVED with their existing hypotheses.
There is no repair wave because no objection requires one, and no further
hostile round. Constant positive rank is retained for general etale arrows;
every finite separable field tower is included. Full decoder histories are
not identified with the binary decoder of a composite isometry. The
embedding-set register has dimension dim_K A, not the cardinality of A.
Nonseparable degree and general-field Fourier/Haar realization are outside
these statements. No full arithmetic-limit theorem is inferred.

PASS — GAL-FUNCTOR, GAL-DESCENT and GAL-IMAGE admitted.
