# Frobenius orbit boundary — capped adjudication, 2026-09-09

User authorization: the next standard verifier/prover round, with uniform
extension and tower scope. The prior root derivation is the prover pass.
Blind native critic: `frobenius-boundary-r1.md`, final FAIL(O1), with no
mathematical counterexample and all four written statements verified within
their stated finite-field scope. Same-family limitation is recorded there.

One repair wave was applied by root; no new hostile pass was launched.

* O1 (MAJOR, checker): B4 now constructs the actual B_(2,2) permutation,
  moves the input state into its multiplicity/cycle coordinates, optionally
  dephases between multiplicities, and transports back. It compares the Born
  return to the fixed independent value one and applies the computed channel
  to every matrix unit. The dephased return is 1/2. The critic's data mutation
  replacing the pure input by its diagonal now fails the Born gate both in
  green mode and with the coherence flag. Green and all six named mutants
  have the required exits. Root execution is frozen in
  `numerics/arithmetic-limits/results/orbit-repair.json`.
* O2 (MINOR, phase restatement): the labbook now uses its named primitive
  root zeta_p in the negative Fourier kernel, matching D1301/D1306, rather
  than specializing it to exp(2 pi i/p). The arbitrary-character overlap
  formula and all theorem scopes are unchanged.

Decision: promote FRL-ORBIT, FRL-POS, FRL-COMP and FRL-ACTIVE to PROVED at
their existing, explicitly stated scopes. The new general Galois/Gram/tower
campaign is separate; this decision supplies no unproved extension to
arbitrary named embeddings, non-finite fields, or the full mixed arithmetic
process category. Definitions D1331--D1334 are unchanged.

The decisive evidence is the structured proofs plus this capped review and
mechanically verified repair. Finite examples alone are not the proof.

PASS — O1 and O2 closed; no open FATAL/MAJOR on the four admitted statements.
