# Planning summary

Planning model: `gpt-5.6-sol`, reasoning `xhigh`.

`BRIEF.md` is a proof-ready work order for SP-SCALAR and SP-CP, intended for
`briefs/phantasm-processes-target.md`.  It records the exact reusable clauses
of admitted FRP-CP, checked SP-WAT18/SP-CK21 locators, three bounded proof
packages, an exact rational finite-check matrix, and sixteen named mutation
paths.

The plan identifies one precise prerequisite repair to D1706: own
outcome-first retained/sequential/tensor indices and the ordinary-trace
adjoint.  It explicitly records that a channel's unital CP adjoint need not
be trace-nonincreasing in the reverse direction.  No new definition number is
needed.

The scalar package keeps D1705's projective class separate from actual CP
maps: an admissibly normed representative must be chosen, general rescaling
changes the branch by `|c|^2`, and zero is never conditionally normalized.
The CP packages cover intrinsic block Kraus exhaustion, ordinary direct-sum
trace, composition/tensor, retained and sequential outcomes, Kraus-list
nonuniqueness, and the adjoint boundary without asserting arithmetic or
stabilizer source exhaustion.

The follow-on priority is one bounded SP-SUM pass, then the arithmetic spine
SP-TRACE to SP-FROB with SP-SUBSYS after SP-CP.  Global action remains blocked
on its named relation-lift, characteristic-two, rig, assembly and arithmetic
dependencies.

This lane contains planning only.  It adds no proof, checker, review, status
change, trunk edit or Git action.
