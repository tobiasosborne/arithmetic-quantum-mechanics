# SP-WEYL critic summary

Blind hostile review completed with critic model `gpt-5.6-sol`, reasoning
`xhigh`. The pre-existing reuse draft does not record its author's exact
model; the campaign lacks a cross-family split, and no prover reasoning or
lane material was read.

**Verdict: PASS.** No FATAL or MAJOR objection landed. The arbitrary-rank
coordinate reduction, negative dual sign, positive half-dot rephasing,
D1703 wavefunction, raw-center quotient (including `q=p^f` and rank zero),
matrix/trace clauses, and inherited unitary Stone–von Neumann scope all
survive independent recomputation.

Two MINOR repairs are recorded:

1. The canonical statement should say “fixed-central-character irreducible
   unitary representations” before asserting unitary equivalence.
2. R4's standalone trace-normalization and vacuum-count subchecks are
   constructed identities. Its exact Gram test remains meaningful, and the
   proof of the trace is correct; the red modes should instead mutate and
   inspect actual operator/trace data.

All eleven bridge mutations exited `1` at R1–R6 as registered, and all
contract mutations exited `1` at G1–G8. The contract results are pinned to
checkpoint `853c564`, checker blob `5c2c94ce442e2fbee44759597612b4340bf64990`,
before the coordinator's later promotion-plumbing edit. Independent
temporary-copy mutation of the imported Abelian operator order died at R2.
Proof-only sign mutations survived both checkers, confirming their documented
implementation/contract scope and the need for the hostile proof-text review.

Full computations, gate paths, fix demands, surviving statements, and the
protected verified-correct scope are in `VERDICT.md`.
