# String-anchored relation-lift planning handoff

Planning model: `gpt-5.6-sol`, reasoning `xhigh`.

This lane makes no canonical, proof, checker, status or Git changes.

Copy `TARGET.md` to the future work-order path
`briefs/phantasm-relation-lift-target.md`. Keep `GENERATOR-MATCH.md` beside
that brief or under the future proof directory as its exact generator
contract; do not merge the table into a claim before its definition owner is
registered.

In `claims/PHANTASM-DAG.md`, anchor on the `DG-REL-LIFT` decision row only
after the coordinator adopts this plan. Record `RL-GEN-MATCH` as the first
bounded output with dependencies

> FRP-CAT,FRP-CP,SP-COMPACT,SP-STAB-REL,SP-SCALAR,SP-CP,SP-EGOROV,SP-TENSOR

and sources `SP-CK21,SP-BC24,SP-WAT18,SP-GROSS06`. Do not mark the decision
gate complete merely by registering or proving this generator comparison.

Before adding `RL-GEN-MATCH` to `claims/CLAIMS.md`, allocate a free definition
number for the exact `G_p^arith` fragment and selected terms in
`GENERATOR-MATCH.md`. Its Scope must exclude the full arithmetic source,
arbitrary complex coefficients, source faithfulness/fullness, higher gates,
extension objects and characteristic two. Restate the definition and claim
exactly in the labbook at `SKETCH`.

The future claim text is the quoted block in `TARGET.md`; use it without
strengthening “generator comparisons” into an equivalence or normalized
functor. A later amplitude-line definition is a separate decision after this
comparison, not part of the same registration patch.

Update the categorical-structure record only to state the open requirement:
Hermitian amplitude lines, coherent composition/tensor/dagger, actual
cup/cap scalars, branch norms, and an `R_p`-form on the matched fragment.
Do not describe these as established properties.
