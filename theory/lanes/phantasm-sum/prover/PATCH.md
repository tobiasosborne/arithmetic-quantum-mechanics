# String-anchored coordinator patch plan

Lane model: `gpt-5.6-sol`, reasoning `xhigh`.

This lane makes no trunk, status, checker, or Git changes. D1707's block
action, empty-list zero action, summand projections and dephasing map are
already canonical and need no prover edit.

The coordinator is registering SP-STAB-REL as a direct SP-SUM dependency.
Preserve it: finite projective relation Hom-sets prove the strict Scope
enlargement, while rectangular spanning remains the direct D1704 matrix-unit
construction.

Copy `sum.md` to `theory/symplectic-phantasm/sum.md` without expanding its
scope. Keep SP-SUM at `SKETCH` for the one blind review.

In `claims/CLAIMS.md`, anchor on the row beginning `| \`SP-SUM\` |`; after
proof integration replace only its draft proof pointer by
`theory/symplectic-phantasm/sum.md` with prover-pass wording. In
`claims/PHANTASM-DAG.md`, anchor on `## SP-SUM` and replace `Proof: none` by
that path. Do not fill Review, say admitted, remove Remaining, or alter the
statement/dependencies before adjudication.

In `labbook/sections/symplectic_phantasm_contracts.tex`, anchor on
`\begin{proposition}[Coherent sums and tagged quantum systems]`.
`LABBOOK-FRAGMENTS.tex` contains the exact proposition and Scope followed by
readable proof prose. Add the proof only with unreviewed provenance visible,
or after adjudication with the final status. The proof includes the strict
Scope enlargement through admitted SP-STAB-REL.

`SOURCE-REUSE.md` records the exact admitted and primary-source scope. The
independent checker lane remains separately owned; observed green/red records
are required before integration but supply finite falsification only.

Preserve these landing boundaries: complex addition lives in D1707's actual
map span, not D1705's quotient; the empty list gets no D1706 channel; and no
biproduct, fusion, rig, Gaussian, distributivity, or source-exhaustion result
is added.
