# Core repaired-candidate summary

The lane constructs a continuous positive type-A Hecke family and a typed
operational envelope with a genuine endpoint evaluation at `q=1`.

The continuous algebra uses the fixed orthonormal regular frame
`b_w(q)=q^(-ell(w)/2)T_w(q)`.  On every compact positive interval its section
algebra is a sup-norm C*-algebra, and the parabolic corners form an ordered
monoidal C*-category.  The germ at one is only a star-category with positive
local representatives; it is not assigned a C*-norm.  Endpoint evaluation is
full.  At prime-power `Q`, the proof identifies every evaluated corner and
normalized corner trace with the corresponding partial-flag intertwiner
algebra and physical trace.

The operational envelope separates the register tensor
`A_alpha tensor A_beta` from the collective corner
`A_(alpha concat beta)`.  Assembly is the corner expectation and split is the
corner inclusion.  Their proved relation is `split o assembly=id` on the
separated register; the reverse order is the generally proper coarse graining
`jE`.  This prevents the false identity-tensor extension that would result
from identifying the two observable algebras.

Morphisms are finite typed planar circuits generated by all normalized state
preparations, discards, effects and POVMs, normalized corner-Kraus
instruments, direct retained collective context/parallel lists,
assembly/split, and explicit classical wiring.  Cartesian product and
singleton outcome wires, bijective relabelings, and spatial flips route a
classical wire past a quantum output.  They type sequential and parallel
instrument histories without adding a quantum block exchange.  Equality is
the explicit presented circuit congruence; for interval labels it is
pointwise coefficient-Gram equality and for germs eventual such equality,
with no continuous environmental unitary required.  A strict ordered
monoidal realization sends circuits to finite-dimensional Heisenberg UCP
maps.  Arbitrary CP maps without retained corner data remain outside the
claimed sufficient class.

Every endpoint state, POVM/effect, corner-Kraus instrument, and individual
finite circuit lifts on some neighborhood.  Raw corner lifts are normalized
by `Ktilde=Khat S^(-1/2)`; state and POVM lifts use positive square-root lifts
and trace/operator normalization.  The result is local existence, with no
canonical global lift and no assertion that arbitrary endpoint relation
diagrams lift through symmetry enhancement.

For every fixed local rank, a determinant minor that is a signed permutation
matrix at one remains nonzero nearby.  It proves locally uniform recovery of
the full Kraus Gram from one retained rank-`2n-1` context.  This supplies the
needed local contextual equality without assuming the existing all-positive-
`q` sketch.

Finite instrument trees have continuous branch and event probabilities.
Conditioning is continuous when the limiting event probability is positive;
an explicit continuous vanishing-success instrument shows the hypothesis is
necessary.  The worked `H_3(q)` protocol has refinement success
`q(q+1)/(1+q+q^2)` for the specified Lüders instrument
`K_s=P_2,K_f=1-P_2`, conditional retained-context return
`(1-2q/(q+1)^2)^2`, and joint endpoint probability `1/6`, with endpoint
conditional full density `3P_2`.

The fixed-fibre apartment compression is explicitly distinguished from
evaluation of continuous sections: it is UCP, but its generator-square
calculation is nonmultiplicative for `Q>1`.

Artifacts:

- `01-continuous-hecke-corners.md`: CLIM-1--4.
- `02-operational-envelope.md`: OPLIM-1--4.
- `03-local-lifting-and-context.md`: LLIM-1--4.
- `04-protocol-continuity-and-example.md`: PLIM-1--3 and executable-evidence map.
- `05-classical-wiring.md`: CWIR-1--3.
- `DEFINITIONS-PROPOSED.md`: D1201--D1218.
- `CLAIMS-PROPOSED.md`: ten candidate rows, all left at `SKETCH` pending the
  capped review.
