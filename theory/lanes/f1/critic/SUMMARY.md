# F1 critic summary

Blind same-family hostile review by `gpt-5.6-sol` (`xhigh`).

The positive finite kernel survives independent recomputation: character
duality, the perfect Heisenberg commutator, full matrix algebra and finite
Stone--von Neumann theorem, exact ring central pushout including `F4` and
characteristic two, the affine-quadratic strict normalizer, and the Fourier
lift all check out.  F1-FUNCT correctly uses the anti-diagonal central product.
The external tensor of two `iota`-central models descends to that product and
again has central character `iota`; the artifact does not claim that a fixed
central-character representation sector is internally tensor closed.

Verdict: `FAIL(O1,O2,O3)`, confined to D1007/F1-CORR.

1. Matrices indexed only by orbit sets do not canonically act on realization
   lines of arbitrary free `mu_N`-torsors; use framed objects or intrinsic
   equivariant/line-valued kernels.
2. `LiftHyp_N` has no declared tensor on morphisms, unit, or coherence, so
   “strong symmetric monoidal” is ill-typed until those are supplied.
3. Over `K_N^cyc ~= Q(zeta_N)`, the projective matrix-intertwiner construction
   is essentially forced by the full matrix algebra, while the only proposed
   geometric content names no actual blueprint/band category and is not
   falsifiable.  Split the algebraic theorem from a concrete geometric
   research target.

F1-FUNCT also has a MINOR categorical packaging defect: its three target
monoidal groupoids should be named, though its displayed central-product,
balanced-smash, and Hilbert-tensor comparisons are correct.

M1--M7 passed; every relevant red mode exited through its intended gate.  An
independent non-generating-character mutation failed at M4, confirming that
the ring hypothesis is live.  The finite theorem rows remain honestly
`SKETCH`; F1-CORR should remain a research target until the three major defects
are repaired.
