# SUMMARY — blind SP-LREL / SP-COMPACT review

Model: `gpt-5.6-sol`, reasoning `xhigh`; same-family blind critic pass.

Verdict: `FAIL(OBJ-1, OBJ-2, OBJ-3, OBJ-4)`.

The core mathematics survived independent recomputation. Coisotropic
reduction gives the correct half dimension; affine empty and nontransverse
cases are handled; category, converse, tensor and graph calculations are
sound; and both compact snakes, name/unname, Boolean scalars and the true
closed loop have the claimed values over every finite field.

Four bounded repairs block admission:

1. Define the direct-sum symmetric monoidal structure on the source affine
   symmetry groupoid before calling its graph assignment symmetric monoidal.
2. Restore D1714's finite-dimensional symplectic `U,V,W` quantifiers in the
   labbook definition fragment.
3. Remove or implement the bare `--red` option that the repository runner
   currently mistakes for a mutation although it only triggers argparse.
4. Add registered compact dagger-swap and snake-wiring mutations. The two
   existing G6 modes are bit-identical and fail before either acceptance path.

Two minor repairs remain: replace G4's duplicated tensor oracle and repair the
nonexistent/noncanonical `lrel.md` and `PROPOSAL.md` citations in `compact.md`.
All twelve named red modes otherwise failed at their registered gates, and
green passed. Independent copy mutations confirmed the G1 data oracle and the
later G6 dagger/snake comparisons are live.
