# Arithmetic-interface planning summary

Planning model: `gpt-5.6-sol`, reasoning `xhigh`.

`BRIEF.md` is a 147-line bounded work order for SP-TRACE, SP-FROB and
SP-SUBSYS after the planned SP-SUM pass. It identifies the exact reusable
FRB-TRACE, FRB-FROB, SP-WEYL/F1-REAL, SP-TENSOR/F1-FUNCT and FRP-CP clauses,
then isolates the remaining arbitrary-rank half-form, named-character,
tower, relative-Frobenius and subsystem-decoder comparisons.

The sole ownership gap is SP-FROB's channel clause: add D1706 and the still
`SKETCH` SP-CP dependency before promotion. No new definition number or body
repair is proposed.

The finite plan includes degree divisible by characteristic, rank zero,
nonstandard base characters, relative rather than absolute Frobenius,
observable inclusion versus ordinary-trace decoding, phase independence and
iterated partial traces. It keeps D1327 support-code success distinct from a
nondegenerate tensor subsystem.

This lane contains planning only and advances the local arithmetic
action/state interfaces required by DG-GLOBAL. It makes no characteristic-two
half-form, new SvN, global-state, modular or spectral claim and changes no
current artifact, trunk file, status or Git state.
