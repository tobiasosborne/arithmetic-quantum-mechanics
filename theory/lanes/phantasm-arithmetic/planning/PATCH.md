# String-anchored planning handoff

Planning model: `gpt-5.6-sol`, reasoning `xhigh`.

This lane changes no canonical definition, claim, proof, checker or status.

Copy `BRIEF.md` to the intended trunk work-order path
`briefs/phantasm-arithmetic-target.md` after the planned SP-SUM pass is
scheduled.

Before SP-FROB proof or promotion, anchor on the canonical SP-FROB row in
`claims/CLAIMS.md` and its `## SP-FROB` DAG node. Add D1706 to Definitions
and SP-CP to Dependencies/depends-on. Extend Reuse by the exact clause:

> SP-CP supplies the ambient unitary-channel typing; covariance, invertibility and finite order are proved from the specified Frobenius permutation.

Restate the dependency/provenance change in the owning labbook proposition
without changing the canonical statement or `SKETCH` status. No definition
body needs repair.

After artifacts exist, proof-path/checker fields should be updated only at
the SP-TRACE, SP-FROB and SP-SUBSYS anchors. Preserve their current statements,
scope restrictions, named-character distinctions and source/support-decoder
boundary through review and adjudication.

The handoff next-task order should read: planned SP-SUM; then SP-TRACE to
SP-FROB, with SP-SUBSYS after SP-CP admission. Do not mark DG-GLOBAL ready
from these local interfaces alone.
