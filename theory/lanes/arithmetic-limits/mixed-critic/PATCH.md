# One bounded repair demand for the mixed package

The critic changes no target files. The verdict is PASS with one MINOR.

In `mixed/CLAIMS-PROPOSED.md`, on the MIX-CHART row, replace the exact
string

    while actual induced Fourier extends to Z and anticommutes with that tangent.

by, for example,

    while the chart Fourier factor extends to Z and anticommutes with that tangent; with the regular logical factor retained, the full operator extends to Z tensor B_i.

The labbook already says `Actual chart Fourier becomes Z` and elsewhere
displays the full tensor factor. Root may add the explicit regular endpoint
`Z tensor B_i` there for consistency, but no change to the mathematical
construction, singular sectors, tower proof, references or checker is required.

After that one repair, root should verify the literal replacement and the
existing full Fourier formula, retain MIX-ALL as CONJECTURE, and adjudicate
the positive claims under their exact hypotheses. No new hostile round is
requested. Preserve the VERDICT's independently verified region.
