STATUS: DONE

All nine objections of `theory/verdicts/wh-kappa-r1.md` are dispatched (`REPAIR-NOTES.md`, objection by objection); OBJ-1 accepted in full and written up as the increment's main **positive** result.
**Theorem WH-BETA:** the assignment depends on a polarizing cocycle `β` as well as on `(κ,ψ)`. At odd `p` the dependence is vacuous — `ω/2` is the unique antisymmetric admissible cocycle and `φ_s(t,v)=(t+s(v,v)/2,v)` is an explicit isomorphism. At `p = 2` it is real, and the separating invariant is intrinsic: `Arf(Q_β) ∈ κ/℘(κ)` (two elements), equivalently the sign in `q^{-1}Σ_v W_β(v)² = ε·1` inside the algebra. Sharper still, the `β`-dependence lives **exactly** at the level-`μ_p` structure: `(algebra, Weyl frame)` is `β`-free at every characteristic (`L-TRIV`), while the finite Heisenberg group of Weyl unitaries is not, and `μ_4` is the price of moving between the two types.
**Theorem WH-SYMM** (OBJ-3) is the second canonicity finding: the `κ`-structure of `V` is not recoverable from the system, whose intrinsic symmetry group is `Sp_{2m}(F_p)`, not `SL_2(κ)`.
OBJ-2: "sections exist" is **REFUTED** and replaced by the Frobenius obstruction plus the sections over the inclusion poset. The verdict's two gifts (OPEN-1 splitting at `q = 2,4`; OPEN-2 `= SL_2(κ)`) were re-derived and re-enumerated before adoption, and are credited in the shard.
**Labels:** 28 rows — 22 PROVED, 4 SKETCH, 1 CONJECTURE, 1 REFUTED. Of the fifteen prove-lane rows at PROVED, all fifteen ids still carry PROVED but only two carry the *statement* they had: the rest have a widened quantifier (Thms 2–5, now every `β ∈ Adm(ω)`), a named scope (`β₀`, or the trace-normalized family), a clause moved into a definition, or — for `WH-FUNCT-b` — a clause split off into the REFUTED row. Seven PROVED rows are new. The four SKETCHes are exactly the statements written **in this repair wave** (Arf classification, the `ε` identity, the level statement, `β`-independence of `P(H_ψ)`): each has a complete proof and exhaustive verification, none has faced a hostile round, and `PRD.md`'s gate to PROVED is procedural. No PROVED row depends on a weaker row.
**Evidence:** five standalone `python3` computations in this lane (`beta_census`, `frame_mu`, `fp_symmetry`, `funct_sections`, `split24`), exact integer arithmetic, no tolerances, sharing no code with prove/check/critic; each green exit 0 with every red mode exit 1 (`./run-all.sh`, logs in `runs/`).
**Outside scope, in `PATCH.md`:** `definitions.md` (D1–D11, `β` promoted to a datum), `notation.md`, `theory/checks/wh_kappa_check.py` (nine anchored edits: OBJ-5's dead `ψ`-isotropy branch repaired and reached in both directions; new gates `C10` β-dichotomy and `C11` `D8`-model; three new red modes — all green, all reds exit 1), `theory/checks/wh_kappa_RED-MATRIX.md` (addendum, saying plainly that `C10`/`C11` are **not** pre-registered and promote nothing).
**Shard:** split in two to respect L2 — `wh-kappa.md` (420 lines, §0–§5 + the shared ledger) and `wh-kappa-choice.md` (503, §6–§11); promotion targets listed in `PATCH.md` Patch 5.
**Remaining:** `WH-WEIL-e` (splitting at general `q = 2^m`) stays CONJECTURE; OPEN-4 (which `μ_4`), OPEN-5 (is any `β` distinguished — the hyperbolic type is the only one admitting `D8`'s model), OPEN-6 (bi-additive cocycles give more than two classes: `n₀ ∈ {1,3,5,7}` at `q = 4`).
