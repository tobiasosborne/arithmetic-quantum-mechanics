STATUS: DONE

Shard `wh-kappa.md` (500 lines, Lamport, **zero `[ADMITTED]` steps**: every theorem is derived, sources corroborate only, so ledger note N3 bites nothing). 16 rows proposed in `CLAIMS-ROWS.md` — 15 PROVED-pending-critic, 1 CONJECTURE.
Proved: WH-FORM (sharpened to `κ`-linear), WH-COMM, WH-ALG (simple, centre `C`, `dim q²`, polarization-free, no cited theorem), WH-ALG-MAT, WH-SVN, WH-POL, WH-CHOICE, WH-CANON (the **projective** Hilbert space is canonical — singleton Hom-sets, so every naturality square commutes; the Hilbert space is not), WH-WEIL-a/b/c/d, WH-FUNCT-a/b.
Char 2, where the increment earns its keep: `ω(v,v)=0` is *computed* (Thm 1 `<1>2`). New: at `p=2` a frame-preserving automorphism over `g` has `μ_p` phases **iff** `g ∈ O(Q)`, `Q(v)=β(v,v)` — proper, order `2(q−1)`: an independent derivation in `V(κ)=κ⊕κ` of the `O(Q) ⊊ Sp(V)` phenomenon of `arxiv-0808.1664:99-110`; and for `g ∉ O(Q)` *every* such automorphism has a phase of order exactly 4. New: the `q−1` lines with `Q|_L ≠ 0` force `μ_4`-valued line characters.
Per N1 no uniform-in-`p` `SL_2(κ)` action is claimed: WH-WEIL-b carries "`p` odd" inside its statement; whether the extension splits at `p=2` is OPEN-1 with three forward attacks. The source's `:722` non-split assertion concerns a *different* extension (kernel `Hom(V,R)`, Witt ring) and is not imported.
Functoriality refuted, not smuggled: `ψ'_ζ|_κ = ψ_{ζ^n}` is trivial when `p|n` (first instance `F_2 ⊂ F_4`), so the trace-normalised family is not restriction-compatible; the surviving functor on pairs `(κ,ψ)` is proved (WH-FUNCT-a/b).
Errata: E1 forced a rewrite of Thm 5 `<1>1`, which now **derives** `W(a,b)=Z(−b)X(a)` from the module induced by `L₀=0⊕κ` (the sign is the phase paid reordering `W(a+y,b)`), backed by lane computation `model-ordering.py` — green, and `--red-naive` exits non-zero while *passing* at `q∈{2,4,8}`, which is E3's point and is recorded in the shard. E2 moved WH-POL to Lagrangian language; E5 moved WH-CHOICE to the `κ^×`-torsor form.
Two brief statements sharpened rather than silently fixed: "the group preserving `ω`" (`κ`-linear only; `F_p`-linear is OPEN-2) and "`q+1` Schrödinger models" (`q+1` lines, `q(q+1)` models, `q` per line).
Outside-scope targets: `PATCH.md` proposes `definitions.md` D1–D9 (anchor `*No definitions yet.*`) and the `notation.md` symbol table (anchor: the empty table), with an anchor-collision fallback for concurrent lanes.
Nothing was written outside `theory/lanes/wh-kappa/prove/`; no git command was run.
