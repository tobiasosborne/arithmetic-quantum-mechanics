# SUMMARY — exact completion-control lane

Model: `gpt-5.6-sol`, reasoning setting `xhigh`.

The lane supplies a standalone exact `phantasm_completions_check.py` proposal
for SP-FOCK, SP-PRIME and SP-BC-CONTROL.  `EXPECTATIONS.md` was frozen before
implementation.  No completion prover artifact was read and no proof, trunk,
status or git change was made.

F1--F3 construct fifteen rational permutation symmetrizers, verify sector
ranks through four in dimensions zero/one/two, vacuum and one-mode factorial
normalization, fifteen binomial exponential norm-square/naturality cases, and
sectorwise contraction functor laws. Actual `tensor_power(2I,r)` matrices act
on normalized symmetric tensors with coefficients 1,2,4,8,16 and squared
norms `4^r`; the tensor-collapse red now reaches these data. The expansive red replaces an actual
accepted contraction by `2I` and fails its `-3I` effect deficit.  Separately,
the values 1,2,4,8,16 are finite witnesses accompanying the symbolic `2^r`
written-proof duty.

P1 checks fourteen increasing-prime inclusions and thirty triangles for local
dimensions 2,3,4, including the empty scalar stage, exact units/star/products,
injectivity and characteristic polynomials under identity insertion.  P2
checks faithful and pure product densities, positivity, compatibility, exact
GNS Gram ranks 576/192/3 and matrix-unit cyclicity.  The separating red replaces
the faithful rho_3 reference by a pure trace-one density and loses actual
Gram-action injectivity.  Separately, a nonzero operator kills the pure cyclic
vector in GNS norm; no claim about representation faithfulness is made.

B1--B3 use positive integer basis indices without truncation, directly check
`mu_n^*(k)=k/n` on multiples and zero on nonmultiples, and use exact rational
phases/root divisibility and prime-valuation energy vectors.  They check the
semigroup, adjoints, proper range projections, time exponent, actual nu corner
star-homomorphism and the concrete failure of multiplicativity for `L_2`.  The
L red substitutes the actual compression into nu's map-product comparison.
B4 computes rational integral-tail intervals for b=2,3 and eight dyadic b=1
lower witnesses.  Infinite trace class and continuity remain written duties.

After the sole checker repair, all fifteen actual-data mutations exit `1` at
their intended gates. The two new paths alter actual tensor-power matrices and
actual adjoint divisibility. Temporarily disabling only either new acceptance
comparison makes its mutation survive with exit `0`. Three earlier
focused disabled guards make the binomial, prime-position and root-average
mutations survive with exit `0`; restored source catches them.  Bare/multiple
usage exits `2`; normal and optimized greens pass in about 0.11/0.26 seconds.

`REPAIR.md` records the bounded checker disposition; no proof repair was
required by the verdict.

Finite success proves none of the completion claims, creates no rig/global/
modular structure, and attaches no operator to zeta zeros.  `RUNS.md` records
exact paths and `PATCH.md` gives anchored integration steps while all statuses
remain `SKETCH`.
