<!-- ROLE: the argument DAG (L5). One row per claim. Status changes only through
     the capped L6 loop of PRD.md; verdict files live in theory/verdicts/.
     Authoritative statuses: PROVED | SKETCH | CONJECTURE | REFUTED.
     Conditional, empirical, under-review and future-work qualifiers belong in
     the statement prose, NEVER as additional status values. -->

# Claims DAG

## Status vocabulary

| status | means |
|---|---|
| `PROVED` | a Lamport-structured proof survived the capped L6 loop with no open FATAL or MAJOR on this statement. A conditional result displays its condition inside the statement and is still written `PROVED`. |
| `SKETCH` | an argument exists and is written down, but the loop ended with an unresolved MAJOR, or a step is admitted. A legitimate terminal state. |
| `CONJECTURE` | believed or wanted, with no argument that survived a round. Evidence from a passing falsifier does not change this. |
| `REFUTED` | a counterexample or a fatal objection landed. The row stays, with the surviving weaker statement recorded beside it. Nothing may depend on a REFUTED row. |

Rules that are not negotiable:

- When in doubt, the label goes DOWN.
- A passing falsifier promotes nothing. A disagreeing falsifier blocks promotion
  unconditionally, even against a proof that passed review.
- No row may be justified by `v0.1/` (L12). Inherited statements are re-derived
  or they are not here.
- Every row's `proved in` and `tested in` cells point at a real file. An empty
  `tested in` is honest; a fabricated one is a FATAL defect.

## Rows

| id | statement (short) | status | depends on | proved in | tested in |
|---|---|---|---|---|---|
| `WH-FORM` | For every finite `κ` (`p = 2` included) `ω` of `D1` is `κ`-bilinear, alternating in the strong sense `ω(v,v) = 0`, and nondegenerate; every `β ∈ Adm(ω)` is a 2-cocycle with `β − β^T = ω`; the group of `κ`-linear automorphisms of `V(κ)` preserving `ω` is `SL_2(κ)`, **and so is the group of merely `F_p`-linear ones** | PROVED | D1, D2 | `theory/wh-kappa.md` §1 | `theory/checks/wh_kappa_check.py` (C1,C2); `theory/checks/fp_symmetry.py` (S1) |
| `WH-COMM` | `W_β(v)W_β(v') = ψ(ω(v,v'))W_β(v')W_β(v)` and `W_β(u)W_β(v)W_β(u)^{-1} = ψ(ω(u,v))W_β(v)`, uniformly in `p` **and for every `β ∈ Adm(ω)`** | PROVED | WH-FORM, D3, D4 | `theory/wh-kappa.md` §2 | `theory/checks/wh_kappa_check.py` (C3,C4) |
| `WH-ALG` | `A_{ψ,β}(V)` is simple with centre `C·1` and `dim_C = q²`, **for every `β ∈ Adm(ω)`** — proved with no polarization, no module and no cited theorem | PROVED | WH-COMM | `theory/wh-kappa.md` §3 | `theory/checks/wh_kappa_check.py` (C5,C6) |
| `WH-ALG-MAT` | `A_{ψ,β}(V) ≅ M_q(C)`, by an isomorphism that depends on a polarization `(L,χ)` and a basis and is **not** canonical | PROVED | WH-ALG, WH-SVN | `theory/wh-kappa.md` §5 `<1>2` | `theory/checks/wh_kappa_check.py` (C5) |
| `WH-SVN` | `H_β(κ)` has exactly one irreducible unitary representation with central character `ψ` up to unitary equivalence; its dimension is `q`; the intertwiner is unique up to `U(1)`; every algebra automorphism is inner | PROVED | WH-ALG, WH-POL | `theory/wh-kappa.md` §5 | `theory/checks/wh_kappa_check.py` (C3,C6,C11) |
| `WH-POL` | Every `κ`-line of `V(κ)` is Lagrangian (because `ω(v,v) = 0`, *computed*), so the Lagrangians are exactly `P¹(κ)`, `q+1` of them; a Schrödinger model is a **pair** `(L,χ)`, there are `q(q+1)` such pairs, `q` per line, **and all of them are isomorphic as `A_{ψ,β}`-modules** — the count is of labels, not of objects | PROVED | WH-FORM, WH-ALG; **final clause only:** WH-SVN `<1>3` (no cycle: WH-SVN uses only WH-POL(a)–(d)) | `theory/wh-kappa.md` §4 | `theory/checks/wh_kappa_check.py` (C7) |
| `WH-POL-2` | **[p = 2]** For every `κ`-line with `Q_β\|_L ≠ 0`, every character of `A_L` takes a value of exact order 4: the model's phases are forced out of `μ_2 = ψ(κ)` into `μ_4`. For the reference cocycle `β₀` the lines with `Q_{β₀}\|_L = 0` are exactly the two coordinate axes, so this applies to `q−1` of the `q+1` lines. **[p odd]** no such phenomenon | PROVED | WH-POL, D6 | `theory/wh-kappa.md` §4 `<1>5`,`<1>6` | `theory/checks/beta_census.py` (B6, `#{Q=0}`) |
| `WH-CHOICE` | Beyond `κ` the construction depends on **exactly two** data: a nontrivial `ψ ∈ X(κ)` and a polarizing cocycle `β ∈ Adm(ω)`. `X(κ)` has `q−1` elements and is a `κ^×`-torsor (a single point at `q = 2`); `Adm(ω)` has `q³` elements and is a `Sym(V)`-torsor. Distinct `ψ` give representations of the *same* group `H_β(κ)` with distinct central characters, hence inequivalent; the algebras are isomorphic but the frame-preserving isomorphisms form a torsor under a group of order `q²·\|SL_2(κ)\|`, so none is distinguished | PROVED | WH-ALG, WH-WEIL-a, WH-BETA-a | `theory/wh-kappa-choice.md` §7 | `theory/checks/wh_kappa_check.py` (C8 in its E5 form, C10) |
| `WH-CANON` | The Hilbert space is canonical only up to a `U(1)` of identifications, but `PMod_{ψ,β}(κ)` has singleton Hom-sets, so the **projective** Hilbert space `P(H_ψ(κ))` is canonical in `(κ,ψ)`: it is the limit over the model groupoid and every transition square commutes because every transition map is unique | PROVED | WH-SVN, WH-CHOICE | `theory/wh-kappa-choice.md` §7 `<1>3`–`<1>4` | (none) |
| `WH-CANON-BETA` | `P(H_ψ(κ))` does not depend on `β` either: a frame isomorphism `A_{ψ,β} → A_{ψ,β'}` transports models and induces the unique isomorphism of limits | SKETCH | WH-CANON, WH-BETA-LEVEL | `theory/wh-kappa-choice.md` §7 `<1>4.<2>1` | (none) |
| `WH-BETA-a` | `Adm(ω)` is a torsor under the symmetric `κ`-bilinear forms and `\|Adm(ω)\| = q³` | PROVED | D1, D2 | `theory/wh-kappa-choice.md` §6 `<1>1` | `theory/checks/beta_census.py` (B1); `theory/checks/wh_kappa_check.py` (C10) |
| `WH-BETA-b` | **[p odd]** the choice of `β` is immaterial: `ω/2` is the unique antisymmetric member of `Adm(ω)` and is `SL_2(κ)`-invariant, and `φ_s(t,v) = (t + s(v,v)/2, v)` is an isomorphism `H_β(κ) → H_{β+s}(κ)` fixing the centre, for every `s ∈ Sym(V)` | PROVED | WH-BETA-a | `theory/wh-kappa-choice.md` §6 `<1>2` | `theory/checks/beta_census.py` (B2,B3,B4); `wh_kappa_check.py` (C10) |
| `WH-BETA-c` | **[p = 2]** `Adm(ω)` has no antisymmetric member, so nothing in the symmetry of `β` distinguishes a point of the torsor | PROVED | WH-BETA-a | `theory/wh-kappa-choice.md` §6 `<1>3` | `theory/checks/beta_census.py` (B2) |
| `WH-BETA-d` | **[p = 2]** `Q_β` is a `κ`-quadratic form with polar form `ω`; in `H_β(κ)`, `(t,v)² = (Q_β(v),0)`, so the element-order profile is `{1:1, 2: q·n₀−1, 4: q³−q·n₀}` with `n₀ = \|Q_β^{-1}(0)\|`; hence `Adm(ω)` meets **at least two** isomorphism classes of Heisenberg group — at `q = 2`, `6` of the `8` cocycles give profile `{1:1,2:5,4:2}` and `2` give `{1:1,2:1,4:6}` (the orchestrator's census identifies these as dihedral and quaternion; this lane computes profiles, not names) | PROVED | WH-BETA-a, D6 | `theory/wh-kappa-choice.md` §6 `<1>4` | `theory/checks/beta_census.py` (B3,B6); `wh_kappa_check.py` (C10) |
| `WH-BETA-TYPE` | **[p = 2]** `κ/℘(κ)` has two elements and `Arf(Q_β) = Q_β(e)Q_β(f) mod ℘(κ)` is basis-free (it vanishes iff `Q_β` has a nonzero zero, and then `n₀ = 2q−1`, else `n₀ = 1`); two members of `Adm(ω)` are `SL_2(κ)`-isometric iff they have equal `Arf`; both types occur, with `q(q+1)/2` and `q(q−1)/2` quadratic forms and `\|O(Q_β)∩SL_2(κ)\| = 2(q−1)` and `2(q+1)`; and `H_β(κ) ≅ H_{β'}(κ)` iff `Arf(Q_β) = Arf(Q_{β'})`. **So `Adm(ω)` meets exactly two classes, indexed by `Arf`** | SKETCH | WH-BETA-d, D10 | `theory/wh-kappa-choice.md` §6 `<1>4`,`<1>5` | `theory/checks/beta_census.py` (B5,B6,B7); `wh_kappa_check.py` (C10) |
| `WH-BETA-EPS` | **[p = 2]** the type is visible inside the quantum system: `W_β(v)² = ψ(Q_β(v))·1` and `q^{-1}Σ_{v∈V}W_β(v)² = ε·1` with `ε = +1` for `Arf = 0` and `ε = −1` otherwise; equivalently `ε = Tr(Arf(Q_β))` read through `κ/℘(κ) ≅ F_2` | SKETCH | WH-BETA-TYPE | `theory/wh-kappa-choice.md` §6 `<1>6` | `theory/checks/beta_census.py` (B6,B8) |
| `WH-BETA-LEVEL` | For all `β,β' ∈ Adm(ω)` there is a frame isomorphism `A_{ψ,β} → A_{ψ,β'}`, `W_β(v) ↦ μ(v)W_{β'}(v)`, at **every** characteristic; `μ` may be taken `μ_p`-valued iff `ψ∘Q_β = ψ∘Q_{β'}`, and at `p = 2` it may always be taken `μ_4`-valued. Hence the pair (algebra, Weyl frame) does not depend on `β`, while the level-`μ_p` frame `F^{(p)}` — the finite group of Weyl unitaries — does, and only at `p = 2` | SKETCH | WH-BETA-a, WH-BETA-d | `theory/wh-kappa-choice.md` §6 `<1>7` | `theory/checks/frame_mu.py` (M1,M2,M3,M4) |
| `WH-WEIL-a` | `1 → V̂ → Aut_F^κ(A_{ψ,β}) → SL_2(κ) → 1` is exact at **every** characteristic, and the kernel consists exactly of the conjugations by Weyl operators | PROVED | WH-ALG, WH-COMM | `theory/wh-kappa-choice.md` §8 `<1>2`–`<1>3` | `theory/checks/split24.py` (X2) |
| `WH-WEIL-b` | **[p odd]** `α_g(W(v)) = ψ(Q_g(v)/2)W(gv)` splits that extension, so `SL_2(κ)` acts on `A_{ψ,β}(V)` by `C`-algebra automorphisms with `μ_p` phases, and by `WH-SVN` this gives a projective unitary representation of `SL_2(κ)` on the model space | PROVED | WH-WEIL-a, WH-SVN | `theory/wh-kappa-choice.md` §8 `<1>4`,`<1>7` | `theory/checks/split24.py` (X4) |
| `WH-WEIL-c` | **[p = 2]** A frame-preserving automorphism over `g` has phases in `ψ(κ) = μ_2` **iff** `g ∈ O(Q_β)`, `Q_β(v) = β(v,v)` — a statement about the chosen `β`, not about the characteristic. For the reference cocycle `β₀`, `O(Q_{β₀}) ∩ SL_2(κ)` consists of `diag(r,r^{-1})` and `antidiag(s,s^{-1})`, has order `2(q−1)` (enumerated at `q = 2,4,8`: `2, 6, 14`), and is **proper** in `SL_2(κ)` | PROVED | WH-WEIL-a, D6 | `theory/wh-kappa-choice.md` §8 `<1>5` | `theory/checks/beta_census.py` (B6); `theory/checks/fp_symmetry.py` (S3) |
| `WH-WEIL-d` | **[p = 2]** For `g ∉ O(Q_β)`, *every* frame-preserving automorphism over `g` has a phase of exact order 4. So `μ_4` is forced exactly when `O(Q_β)∩SL_2(κ)` is proper — for `β₀` at every `q` (`WH-WEIL-c`), and for **every** `β` at `q = 4, 8` by direct enumeration — and it is **not** forced in the case `q = 2` with `β` of anisotropic type, where the `μ_2`-phase subgroup is all of `Aut_F^κ` (verified directly). `μ_4` is therefore **not** a characteristic-2 invariant of `(V,ω,ψ)`. *(The general-`q` order `2(q+1)` for the anisotropic type is `WH-BETA-TYPE`; this row does not use it.)* | PROVED | WH-WEIL-c | `theory/wh-kappa-choice.md` §8 `<1>6` | `theory/checks/split24.py` (X4,X6 + the anisotropic cross-run) |
| `WH-WEIL-e` | **[p = 2]** The extension of `WH-WEIL-a` splits for every `q = 2^m`, i.e. `SL_2(κ)` acts on `A_{ψ,β}(V)` by `C`-algebra automorphisms. Verified for `β₀` at `q = 2` (4 complements, exhaustive over generating pairs) and `q = 4` (64 lifts of a `(2,3,5)` presentation), in both cases necessarily with `μ_4` phases; **no argument for general `q`** | CONJECTURE | WH-WEIL-a, WH-WEIL-d | — | `theory/checks/split24.py` (X4,X5,X6) |
| `WH-SYMM` | `A_{ψ,β}(V)` and its frame are built from `(V,+)` and `ψ∘β` alone, so the `κ`-module structure of `V` is **not** recoverable from them; dropping `D7`'s imposed `κ`-linearity, `Aut_F(A)` surjects with kernel `V̂` onto the isometry group of the `F_p`-valued alternating form `ψ∘ω`, which strictly contains `SL_2(κ)` whenever `m > 1` — orders `6,24,720,120,1451520,51840` and indices `1,1,12,1,2880,72` at `q = 2,3,4,5,8,9`, by complete enumeration | PROVED | WH-WEIL-a, WH-FORM | `theory/wh-kappa-choice.md` §9 | `theory/checks/fp_symmetry.py` (S1,S2,S3) |
| `WH-FUNCT-a` | **The trace-normalized family with a common `ζ`** is not compatible with field extension: for `κ ⊆ κ'` of degree `n`, `ψ'_ζ\|_κ = ψ_{ζ^n}`, trivial whenever `p \| n` (first instance `F_2 ⊂ F_4`). *Scoped: this quantifies over the trace family only; the statement for all families is `WH-FUNCT-c`* | PROVED | D3 | `theory/wh-kappa-choice.md` §10 `<1>1` | `theory/checks/funct_sections.py` (F1) |
| `WH-FUNCT-b` | On the category `FF^±` of pairs `(κ,ψ)` with character-compatible embeddings, `(κ,ψ) ↦ A_{ψ,β₀}(V(κ))` **is** a functor to unital `C`-algebras and injective homomorphisms, respecting composition and identities | PROVED | WH-ALG | `theory/wh-kappa-choice.md` §10 `<1>2` | `theory/checks/funct_sections.py` (F5) |
| `WH-FUNCT-b-SEC` | *(prove-lane row, kept per `PRD.md`)* Sections of `FF^± → {finite fields}` exist | **REFUTED** | — | — | `theory/checks/funct_sections.py` (F2,F3) |
| `WH-FUNCT-c` | There is **no** choice of `ψ_κ ∈ X(κ)`, one per finite field, compatible with all field embeddings: Frobenius forces `ψ_κ = ψ_c` with `c ∈ F_p`, and for `κ = F_{p^p}` every such character restricts trivially to `F_p`. First instance `F_2 ⊂ F_4` | PROVED | D3, WH-FUNCT-a | `theory/wh-kappa-choice.md` §10 `<1>3` | `theory/checks/funct_sections.py` (F2,F3) |
| `WH-FUNCT-d` | Over the inclusion poset of the subfields of a fixed `\bar F_p`, the compatible systems `(ψ_κ)` are **exactly** the restrictions of the additive characters `Ψ` of `(\bar F_p,+)` with `Ψ\|_{F_p} ≠ 1` | PROVED | D3 | `theory/wh-kappa-choice.md` §10 `<1>4` | `theory/checks/funct_sections.py` (F4) |
