# Symplectic Phantasm bootstrap validation — 2026-09-09

The source/definition/planning bootstrap passed its mechanical contracts.
No new mathematical proposition is promoted, and no mathematical falsifier
for the fourteen new lemma sketches has been implemented or run.

- **25 local primary sources:** 19 TeX payloads and six PDFs. Five historical
  leads remain GAP and support no lemma or decision gate. Raw and readable
  source hashes are pinned in the source ledger.
- **13 definitions, 14 SKETCH lemma contracts, seven OPEN decision gates.**
  The contracts resolve their declared inputs, outputs, scope, choices,
  sources and dependencies. The graph is acyclic and the work-plan
  prerequisites agree with it. Input/output prose is not symbolically
  type-checked or mathematically proved.
- **Final contract suite:** one green run and all 15 advertised red modes
  rejected at their intended gates. `CONTRACT.json` contains the commands,
  exit codes, output and final input hashes, after the Joyal gap was resolved.
- **First observed checker run:** the dependency-cycle mutation exited 1 at
  G3, before the first green run. A separate canonical-claim mutation in a
  real temporary file copy failed at G6; see `COPY-MUTATION.txt`.
- **Full repository session-close:** 26 green suites and 253 advertised red
  runs passed. The source-registration refinement was followed by the final
  quest contract rerun above; unchanged mathematical checkers were not rerun.
  `SESSION-CLOSE.json` and `SESSION-CLOSE.txt.gz` preserve the full run.
- **Labbook:** a real latexmk/pdflatex build produced the 173-page PDF;
  Section 31 contains the exact definition and claim restatements. The final
  lockstep gate passed. `BUILD.txt.gz` and `LOCKSTEP.txt` preserve those checks.
  Pre-existing typesetting warnings remain; the new section's overfull
  lines were repaired, and its opening page was visually inspected.

The first proof work order is SP-WEYL, followed by SP-EGOROV and SP-TENSOR.
Actual mathematical promotion still requires a structured proof, exact
falsifiers where applicable, the capped hostile review and adjudication.

The two compressed logs preserve the original transcript bytes without
whitespace edits; raw and archive hashes are recorded in the summary JSON.
