# R18 Review

## Scope

Reviewed only:

- `report/sections/34_f3_polynomial_quotient_arithmetic_fields.tex`
- `report/sections/35_tn_equals_1_f3_arithmetic_field.tex`

Read the mega-review prompt and required repository maps/conventions. Used only
read-only text inspection commands plus this assigned output write. Did not run
Julia, `make test`, `scripts/run_all.jl`, or producer scripts.

## Findings

- Severity: Major
  Location: `report/sections/35_tn_equals_1_f3_arithmetic_field.tex:56`
  Issue:
  The cyclotomic finite-field factor-degree formula is presented as checked
  report content, but the shard says no new external source is used and does
  not cite a local source or prior local derivation for the required finite
  field/root-of-unity facts.
  Evidence:
  Lines 56--68 introduce primitive `d`-th roots, `o_d=ord_d(3)`, Frobenius
  orbit degrees, and the count `varphi(d)/o_d` irreducible factors. The stated
  source anchors at lines 17--21 cite only AQM-29, AQM-34, and AQM-28 for
  polynomial prime classification, residue fields, and Weyl systems; those do
  not establish the primitive-root/Frobenius-orbit factorization theorem. This
  also propagates to the shard skeleton at lines 31--32 and to the summary
  claim in `report/sections/02_lab_log.tex:164`.
  Suggested action:
  Either add a registered local source with precise line/theorem locators for
  factorization of `t^m-1` over finite fields, or expand this into a fully
  local derivation with sourced finite-field prerequisites. Until then, weaken
  the `ord_d(3)` factor-degree count to a pending/check-needed statement while
  keeping the directly derived squarefree/repeated-factor statement.

- Severity: Minor
  Location: `report/sections/34_f3_polynomial_quotient_arithmetic_fields.tex:84`
  Issue:
  The central Chinese-remainder and reduced-quotient step lacks a precise local
  source locator in the shard's source anchors, even though the proof invokes
  the Chinese remainder theorem by name.
  Evidence:
  Lines 84--111 prove `R_p ~= product_i F_3[t]/(pi_i^e_i)` and
  `(R_p)_red ~= product_i K_i` using "Chinese remainder" at lines 98--110.
  The source anchors at lines 13--19 cite Stacks lines for `Spec`, prime
  ideals, residue fields, and opens, but not the local Chinese-remainder lemma.
  A suitable local source exists at
  `references/algebraic_geometry/stacks_project_algebra.tex:2615`.
  Suggested action:
  Add the Chinese-remainder locator to the source-anchor paragraph or make the
  CRT argument fully self-contained. This is a provenance/citation gap, not a
  mathematical counterexample.

## Cross-Shard Risks

The AQM-35 `ord_d(3)` count is repeated in the lab log and handoff summaries.
If the factor-degree provenance is repaired or weakened in AQM-35, sweep
`report/sections/02_lab_log.tex` and `HANDOFF.md` for the same claim.

AQM-34/AQM-35 otherwise keep the reduced residue-qudit layer separate from the
nilpotent-sensitive layer: repeated factors are described as nilpotent
scheme structure invisible to reduced closed-point Weyl labels, with AQM-36
named as the separate thickened layer. I did not find a local counterexample to
the reduced-site counts or the `t^n-1=(t^m-1)^{3^{v_3(n)}}` repeated-factor
logic.

## Suggested Actions

1. Register or cite a finite-field factorization source before keeping the
   `varphi(d)/ord_d(3)` count as checked report content.
2. Add the Stacks Chinese-remainder locator to AQM-34's source anchors.
3. After source/provenance edits, update the lab-log/handoff summaries that
   repeat the affected AQM-35 claim.
