# R45 Review

## Scope

Reviewed only:

- `report/sections/88_derivative_constraints_residue_weyl.tex`
- `report/sections/89_scheme_incidence_code_sources.tex`

I followed the hard command rule: no Julia, no `make test`, no producer scripts. I used read-only inspection of the review prompt, report maps, `report.tex`, `CONVENTIONS.md`, `INDEX.md`, `HANDOFF.md`, the two assigned shards, and relevant local source manifests/source snapshots.

## Findings

- Severity: Major
  Location: `report/sections/89_scheme_incidence_code_sources.tex:198`
  Issue:
  The Shor homological-family claim is source-local but lacks a precise path/line locator in the shard, and the cited source manifest does not currently list the relevant Shor-family locators.
  Evidence:
  The shard says Bombin--Martin--Delgado's local source says Shor's original `[[9,1,3]]` code belongs to a `[[d^2,1,d]]` homological family at `d=3`. The local source does contain this: `references/toric_code/bombin_martin_delgado_0605094_source/HomologicalErrorCorrection6.tex:320-321` and again at `:2492-2493`. However, `references/toric_code/SOURCES.md` only lists locators around toric/homological conventions and original toric codes, not these Shor-family lines. This breaks the repository's precise citation rule for a nontrivial code-source claim.
  Suggested action:
  Add the exact BMD locators to the shard and to `references/toric_code/SOURCES.md`, or weaken the sentence to a deferred/source-to-register note until the manifest is updated.

- Severity: Minor
  Location: `report/sections/89_scheme_incidence_code_sources.tex:52`
  Issue:
  The Artinian-spectrum source statement is not given a usable local locator.
  Evidence:
  The shard says the Stacks Artinian source records that an Artinian ring has finitely many maximal ideals, all primes are maximal, and finite discrete spectrum after local decomposition. The local Stacks snapshot supports the first two pieces at `references/algebraic_geometry/stacks_project_algebra.tex:12728-12765`, but AQM-89's source-anchor paragraph only names affine anti-equivalence and finite products/fibre products in `stacks_project_schemes.tex:841-1004`. `references/algebraic_geometry/SOURCES.md` also does not list the Artinian locators.
  Suggested action:
  Add the Artinian `stacks_project_algebra.tex:12728-12765` locator to the shard/source manifest, or cite AQM-40 for the concrete `Spec(k^n)` point and avoid the broader Artinian source claim here.

- Severity: Minor
  Location: `report/SHARD_CATALOG.md:807`
  Issue:
  The catalogue summary compresses different evidence statuses into "checked Weyl-label targets" and risks overstating the code-construction evidence.
  Evidence:
  AQM-89 itself is more careful: Reed-Muller uses a displayed local degree-filtration convention and says stronger code claims need sources/runs (`report/sections/89_scheme_incidence_code_sources.tex:124`, `:157-159`); Shor is a sourced incidence target but not yet a natural scheme (`:195-200`); toric uses extra group law/generator/face choices and warns against claiming the bare scheme produces the code (`:254-267`). The catalogue phrase makes these sound uniformly checked as code-source constructions.
  Suggested action:
  Rephrase the catalogue line to distinguish "locally checked Weyl-label selections" from "source/proposal routes", e.g. "Compares Steane, Reed-Muller, Shor, and toric candidate source routes, with only the displayed Weyl-label selections checked."

- Severity: Question
  Location: `report/sections/89_scheme_incidence_code_sources.tex:202`
  Issue:
  The toric constant-group-scheme section does not say whether the newly registered Haah/lattice-stabilizer sources are intentionally out of scope.
  Evidence:
  `INDEX.md:203` registers `references/lattice_stabilizer_codes/SOURCES.md` for Haah source bundles on translation-group algebra, free-module Pauli Hamiltonians, lattice-code lecture notes, toric-code maps, and 2D classification. AQM-89's toric section gives an elementary finite Cayley-square chain-complex derivation and cites AQM-05/AQM-06, but it does not cite or explicitly defer Haah/Laurent-polynomial provenance. This is fine if AQM-89 is only the finite incidence precursor, but later readers could mistake it for the sourced lattice-stabilizer/Laurent bridge.
  Suggested action:
  Add one sentence either citing the relevant Haah manifest locators for translation-invariant lattice-stabilizer framing, or explicitly saying the Haah/Laurent-polynomial layer is deferred to AQM-91 and is not used in AQM-89.

## Cross-Shard Risks

AQM-88 is consistent with convention `(aw)`: derivative predicates end as subsets `C_P(S,F) <= E_S` and selected/rejected Weyl-Heisenberg elements via `pi^{-1}`, not as informal derivative diagnostics. The `Z/6Z`, dual-number, product, and `F_3[t]/(t^2(t-1))` examples checked against the displayed algebra and Stacks differential locators without finding a derivative/residue-label mismatch.

The main cross-shard risk is status drift around AQM-89. It is careful in the body about extra incidence/group-law choices, but map/catalogue language and missing source locators could let "candidate source route with checked label subgroup" become "scheme constructs the code." The Haah/Laurent source layer should also be explicitly separated from this finite Cayley-incidence precursor.

## Suggested Actions

Fix the two missing source-locator chains in AQM-89, soften the catalogue summary for AQM-89, and add an explicit Haah/Laurent deferred-or-cited sentence in the toric subsection. No AQM-88 correction is indicated from this review slice.
