# 2026-05-26 Finite Ring Database

## Question

Record the finite-ring database MVP run bundle, including the schema-only
SQLite smoke build, audit gate, and in-memory MVP CSV review exports, before
any sourced ring-row insertion is claimed.

## Command

```bash
julia --project=. scripts/arithmetic/finite_ring_db_build.jl \
  --run runs/2026-05-26-finite-ring-database \
  --max-order 15 \
  --sources gap-small,manual-examples \
  --force

julia --project=. scripts/arithmetic/finite_ring_db_audit.jl \
  --db runs/2026-05-26-finite-ring-database/data/finite_rings.sqlite

julia --project=. scripts/arithmetic/finite_ring_db_exports.jl
```

## Outputs

- `data/finite_rings.sqlite` is generated by the build, local to this run
  bundle, and ignored by git under convention (an).
- The audit gate reads `data/finite_rings.sqlite` and writes no output file in
  this MVP slice.
- `data/ring_summary.csv` is an in-memory MVP review export with one row per
  deduplicated representative.
- `data/ring_presentations.csv` is an in-memory MVP review export with one row
  per local MVP presentation.
- `data/ring_isomorphism_certificates.csv` records verifier-approved MVP merge
  certificates only.
- `data/ring_quantization_summary.csv` exports the residue and
  thickened-Frobenius MVP helper rows.
- `data/ring_quantization_obstruction.csv` is the obstruction-only subset of
  the quantisation export.

## Headline Finding

The current smoke build performs a schema-only SQLite migration and records one
`build_run` row. The audit currently writes no file and passes the schema-only
smoke artifact while enforcing canonical JSON and populated-row semantic checks.
The CSV files are in-memory MVP review exports produced from local helper data;
they do not read or populate `finite_rings.sqlite`. The SQLite build remains
schema-only, writes no certified ring rows, and makes no completeness claim for
finite commutative rings.

## Acceptance Bundle

Acceptance recorded for the current schema-only smoke scope:

| Command | Outcome |
|---|---|
| `julia --project=. scripts/run_all.jl --fast` | PASS: `12 ok, 0 failed, 0 skipped`; the finite-ring build, audit, and exports ran before the existing fast producers. |
| `git diff --check` | PASS: clean. |
| `julia --project=. -e 'using Pkg; Pkg.test()'` | PASS; the existing GAP availability check remains a Broken/`@test_skip` marker because this host has no GAP executable. |

Audit summary from the orchestrator run:

```text
finite_ring_db_audit.jl: ok ... tables=11/11 schema_version=1 build_runs=1 json_cells=2
```

SQLite scope:

- `data/finite_rings.sqlite` is local to this run bundle and ignored by git
  under convention `(an)`.
- The SQLite file is schema-only: `build_run=1`, `ring=0`, and
  `presentation=0`.
- No populated or audited finite-ring catalogue is present in SQLite.

CSV review-export data rows:

| CSV | Data rows |
|---|---:|
| `data/ring_summary.csv` | 12 |
| `data/ring_presentations.csv` | 13 |
| `data/ring_isomorphism_certificates.csv` | 1 |
| `data/ring_quantization_summary.csv` | 26 |
| `data/ring_quantization_obstruction.csv` | 18 |

This acceptance bundle accepts only the schema-only SQLite smoke build, audit
gate, and in-memory MVP CSV review exports. It is not a populated or audited
finite-ring database, and it makes no completeness claim for finite
commutative rings.

## Next Steps

Add persisted audit output, report consumption, MVP review CSV auditing, and
sourced SQLite data insertion in later substeps.
