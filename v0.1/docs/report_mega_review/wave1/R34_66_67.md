# R34 Review

## Scope

Reviewed only:

- `report/sections/66_quantum_system_association_test_object_catalogue.tex`
- `report/sections/67_quantum_system_association_finite_set_first.tex`

Read the required review prompt, report maps, conventions, QSA plan anchors, and relevant local run/readme or CSV anchors by read-only inspection only. I did not run Julia, `make test`, `scripts/run_all.jl`, or producer scripts.

## Findings

- Severity: Minor
  Location: `report/sections/66_quantum_system_association_test_object_catalogue.tex:61`
  Issue: The finite-set catalogue row still says Step 03--06 derivations must fix the empty-set carrier, empty tensor product, empty direct sum, and Gram-choice conventions, but AQM-67 now fixes the Step 03 part for the first formal carrier and Gram caveat.
  Evidence: AQM-66 line 61 leaves the empty-set row at `question` pending Step 03--06. AQM-67 lines 57--68 derives `C<emptyset> = 0` and states there are no basis labels or Gram entries; AQM-67 lines 93--108 records Gram matrices as extra data. The row is not mathematically false if read in report order, but it is stale as a current status matrix for the paired shards.
  Suggested action: Split the finite-set status by workflow, or add a short note that AQM-67 closes the Step 03 formal-carrier/Gram part while tensor-site and direct-sum edge cases remain for later workflows.

- Severity: Question
  Location: `report/sections/66_quantum_system_association_test_object_catalogue.tex:28`
  Issue: The status column allows composite entries such as `question / available`, `available / blocked`, and `available / question`, but the row-local vocabulary defines the individual words rather than the semantics of combinations.
  Evidence: AQM-66 lines 28--32 define `available`, `question`, and `blocked`; table rows use composites at lines 62, 84, 100, 104, and 117--119. The evidence cells usually clarify which workflow is available or blocked, so this is not an overclaiming defect by itself. The risk is that later synthesis may read `available` as a row-level comparison status rather than a per-workflow evidence status.
  Suggested action: Either define composite statuses explicitly or replace the single status column with per-workflow status entries, especially for rows where one workflow is anchored and another remains blocked or only proposed.

## Cross-Shard Risks

AQM-67 itself follows the local convention `(aq)` closely: it treats the bare finite set as supplying only labels, derives the empty, one-point, and displayed `n`-point cases from the formal direct-sum definition, and explicitly refuses to select an identity Gram matrix or Hilbert inner product.

The main cross-shard risk is downstream reuse of AQM-66's broad `available` rows. In AQM-66, `available` means at least one local workflow anchor exists, not that all planned workflows for the object have been compared or that a QSA agreement/inequivalence result has been proved.

## Suggested Actions

Update AQM-66's finite-set rows after AQM-67: mark the first carrier and Gram caveat as available/checked by local derivation, while leaving tensor-site, direct-sum, and all-map comparison claims pending their own shards.

Keep AQM-67's Gram language as written. It avoids the main overclaiming hazard by making `(X,G)` the metric input and keeping the identity Gram matrix out of the bare finite-set association.
