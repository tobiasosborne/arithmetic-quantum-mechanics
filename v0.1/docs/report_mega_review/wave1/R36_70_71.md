# R36 Review

## Scope

Reviewed only:

- `report/sections/70_quantum_system_association_and_or_many_particle_grammar.tex`
- `report/sections/71_quantum_system_association_symplectic_field_labels_set.tex`

Focus followed the assignment: finite AND/OR grammar, tensor/coexistence versus
direct-sum/alternative, finite particle-count sectors, `Map(X,V)` and
finite-support labels, pointwise pairing/radicals, Weyl labels, and checked
`F_3` CSV-row references. I used read-only text inspection only and did not run
Julia, `make test`, `scripts/run_all.jl`, or producer scripts.

## Findings

No concrete issues found in the assigned shards.

Checked risks:

- AQM-70 keeps the finite grammar within convention `(at)`: `AND` is tensor
  coexistence, `OR` is direct-sum alternatives, parse trees are finite, and the
  particle-count grading is explicitly grammar bookkeeping rather than
  statistics, dynamics, Fock completion, or fusion-category structure.
- AQM-70 does not conflate the AQM-68 tensor-site workflow with the AQM-69
  one-particle direct-sum workflow; it also leaves observable-algebra choices
  open where `End(V(E))`, block-preserving direct-sum algebras, and tensor-site
  algebras differ.
- AQM-71 uses `Map(X,V)` rather than `Hom(X,V)` for unstructured finite sets,
  and reserves finite-support direct sums for the closed-point affine layer in
  AQM-28/convention `(y)`.
- AQM-71 correctly carries the radical caveat from AQM-14/convention `(q)`:
  for a chosen subspace `E`, the Weyl label space is `E/rad(E)` unless the
  radical is zero.
- The quoted `F_3` summary rows in AQM-71 match
  `runs/2026-05-24-arithmetic-quantum-fields/data/arithmetic_quantum_field_examples.csv`
  for `finite_two_points_all_maps`, `finite_set_3_full`,
  `vector_space_F3_affine`, `affine_plane_F3_linear`, and
  `parabola_F3_degree_le_1`.
- The quoted basis/Gram rows for `vector_space_F3_affine` and
  `parabola_F3_degree_le_1` match
  `runs/2026-05-24-arithmetic-quantum-fields/data/arithmetic_quantum_field_bases.csv`.
- The run bundle, schema, and index references for
  `arithmetic_quantum_field_examples.csv` and
  `arithmetic_quantum_field_bases.csv` are present and agree with AQM-71's
  description.

## Cross-Shard Risks

The main downstream risk is overreading AQM-70's particle-count sectors as
bosonic or fermionic sectors. The shard currently guards against that at
`report/sections/70_quantum_system_association_and_or_many_particle_grammar.tex:101`
and again at lines 168--187.

For AQM-71, the useful guardrail is that all new finite set, weighting, radical,
carrier-count, or comparison examples require a new local source, derivation,
or run row. The boundary statement at
`report/sections/71_quantum_system_association_symplectic_field_labels_set.tex:166`
keeps that constraint explicit.

## Suggested Actions

No mandatory action for AQM-70 or AQM-71 from this review.

For later synthesis, preserve the current distinction between finite grammar
sectors and genuine statistics/Fock enhancements, and preserve AQM-71's rule
that unreduced field spaces are not honest Weyl label spaces until the radical
has been checked.
