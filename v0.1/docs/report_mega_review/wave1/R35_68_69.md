# R35 Review

## Scope

Reviewed only:

- `report/sections/68_quantum_system_association_finite_set_tensor_sites.tex`
- `report/sections/69_quantum_system_association_finite_set_direct_sum_particle.tex`

Read the required review prompt, report maps, conventions, QSA plan anchors, and the cited local anchor shards by read-only inspection only. I did not run Julia, `make test`, `scripts/run_all.jl`, or producer scripts.

## Findings

- Severity: Minor
  Location: `report/sections/68_quantum_system_association_finite_set_tensor_sites.tex:77`
  Issue: The tensor-by-identity inclusion is written as a strict map between unordered finite tensor products, and the composition law is written as strict equality, but the shard does not name the coherence/strictification convention that makes this more than "up to canonical associativity and symmetry isomorphism."
  Evidence: AQM-68 lines 44--46 say an ordering may be used only to write tensor products and is not structure supplied by the set. Lines 77--88 then write `a \mapsto a\otimes 1_{T\setminus S}` and `\iota_{T,U}\circ\iota_{S,T}=\iota_{S,U}` strictly. The prior Zariski-net anchor AQM-24 explicitly fixes an ordering of the finite set "only to make tensor products strict" at `report/sections/24_zariski_opens_observable_nets.tex:70`, but AQM-68 does not repeat or replace that strictification sentence for the structureless finite-set workflow.
  Suggested action: Add one sentence before the inclusion formula saying the indexed tensor products are interpreted with the standard coherent finite tensor-product convention, or that a temporary displayed ordering is fixed to write the maps and the result is independent up to the canonical symmetric-monoidal identifications.

- Severity: Minor
  Location: `report/sections/68_quantum_system_association_finite_set_tensor_sites.tex:50`
  Issue: The tensor-site shard records the empty tensor product but does not explicitly work the one-point tensor-site edge case, even though the surrounding QSA catalogue treats empty and one-point finite sets as standard boundary tests and AQM-69 works both edge cases explicitly.
  Evidence: AQM-68 lines 56--59 give `H(emptyset)=C` and `A(emptyset)=C`, and lines 64--73 give the displayed `n`-point dimension count. There is no explicit paragraph saying that for `X={*}` the workflow returns exactly the chosen `H_*` or `A_*`, and therefore the one-dimensional result occurs only if that site factor was chosen one-dimensional. AQM-69 lines 88--108 does make the corresponding empty and one-point direct-sum cases explicit.
  Suggested action: Add a short one-point paragraph to AQM-68 parallel to AQM-69: for `X={*}`, `H(X)=H_*` and `A(X)=A_*`, so no one-dimensional or qudit dimension is determined by the bare singleton without the chosen site data.

## Cross-Shard Risks

AQM-68's uniform-qudit review anchors are supported locally: AQM-14 supplies the finite field-label/radical workflow, AQM-22 separates `Spec F_p` from the underlying `p`-element set and proves the all-map `p`-qudit Pauli anchor, AQM-24/AQM-25 supply tensor-by-identity local-net anchors, and AQM-40 supplies the `Spec(k^n)` ambient `n`-qudit residue-field anchor.

AQM-69 follows convention `(as)` closely. It keeps direct-sum alternatives separate from tensor-site coexistence, works the empty and one-point cases, avoids selecting an observable algebra, and compares with AQM-67 only after choosing one-dimensional internal carriers and nonzero or unit vectors.

The main downstream risk is not a false claim in AQM-69, but strict tensor notation in AQM-68 being reused later as if the bare set supplied an ordering or literal inclusions into a fixed ambient tensor product. That should be handled by an explicit coherence sentence rather than by changing the construction.

## Suggested Actions

Clarify AQM-68's tensor-product strictification/coherence convention at the inclusion formula.

Add the missing one-point tensor-site edge case to AQM-68 so the finite-set boundary tests are parallel across AQM-67, AQM-68, and AQM-69.
