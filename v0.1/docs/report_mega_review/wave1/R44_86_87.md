# R44 Review

## Scope

Reviewed only:

- `report/sections/86_quantum_system_association_degenerate_overrestrictive_cases.tex`
- `report/sections/87_quantum_system_association_open_problems_next_targets.tex`

I followed the hard command rule: no Julia, no producer scripts, no tests.  I used read-only inspection of the assigned shards, required review scaffolding, maps, `INDEX.md`, adjacent QSA anchor shards, and already-existing CSV/run README artifacts where cited by the assigned shards.

## Findings

- Severity: Minor
  Location: `report/sections/86_quantum_system_association_degenerate_overrestrictive_cases.tex:72`
  Issue:
  The affine-map radical example cites `\path{data/arithmetic_quantum_field_examples.csv}`, but that file does not exist at top level.  The indexed local artifact is under the run bundle.
  Evidence:
  `INDEX.md:72` lists `runs/2026-05-24-arithmetic-quantum-fields/data/arithmetic_quantum_field_examples.csv`, and that CSV contains the cited rows: `vector_space_F3_affine` with radical dimension `2` and reduced phase dimension `2`, and `affine_plane_F3_linear` with radical dimension `4` and reduced phase dimension `0`.  Top-level `data/` contains `README.md` and `SCHEMA.md`, not the CSV.
  Suggested action:
  Replace the citation with `\path{runs/2026-05-24-arithmetic-quantum-fields/data/arithmetic_quantum_field_examples.csv}`, matching AQM-71 and `INDEX.md`.

## Cross-Shard Risks

No overclaiming issue found in the assigned shards on the requested themes.  AQM-86 keeps collapsed presentations, radical quotients, zero cotangent outputs, nilpotent-forgetting, mixed-residue not-applicable readings, infinite-boundary completions, and blocker labels framed as local catalogue statuses rather than no-go theorems.  AQM-87 keeps the next-target ledger in question/proposal language and does not promote the QSA pass to an equivalence/classification result.

The main residual risk is citation precision: the shards rely heavily on adjacent shard labels rather than equation/table locators.  That is mostly acceptable for a synthesis/gap shard, but the one direct CSV path above should be corrected because it breaks the reproducibility chain.

## Suggested Actions

Fix the stale CSV path in AQM-86.  No report-math correction is otherwise indicated from this review slice.
