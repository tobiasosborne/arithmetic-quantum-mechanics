% SHARD-ID: AQM-63-EVALUATED-DERHAM-CONSTRAINTS-TANGENT-WEYL
% SHARD-TITLE: Evaluated de Rham Constraints on Tangent-Weyl Kinematics
% SHARD-SUMMARY: Reformulates exact de Rham one-forms as cotangent Weyl-operator labels in the tangent-cotangent representation.
% SHARD-SUMMARY: Proves the compatibility statement and separates it from a full CSS rule requiring extra tangent labels.
% SHARD-SUMMARY: Works through finite examples showing that unrestricted exact forms often select a one-dimensional stabilizer subspace.
% SHARD-KEYWORDS: de Rham, tangent Weyl, cotangent constraints, stabilizer, F3, circle, affine line, crossing

\section{Evaluated de Rham Constraints on Tangent-Weyl Kinematics}
\label{sec:evaluated-derham-constraints-tangent-weyl}

\subsection{Status and Source Anchors}

This shard refines the compatibility question left open by AQM-60--AQM-62.
The tangent-cotangent kinematics first constructs the full symplectic label
space and its Weyl-Heisenberg projective representation.  Exact one-forms from
AQM-54 give a possible stabilizer-selection rule only after they are evaluated
as cotangent labels at finite-residue points.  The old finite \(C^1\)-CSS rule
remains a prototype; the fibrewise rule below is the part compatible with the
corrected tangent-Weyl principle.

\subsection{Definition: Evaluated Exact-One-Form Constraints}

Let \(X=\Spec A\), and let \(S\) be a finite set of finite-residue points.  Put
\[
  K_P=\kappa(P),\qquad V_P=T_PX,\qquad V_P^*=T_P^*X
\]
and
\[
  V_S=\bigoplus_{P\in S}V_P,\qquad
  V_S^*=\bigoplus_{P\in S}V_P^*,\qquad
  E_S=V_S\oplus V_S^*.
\]
Let
\[
  \rho_S:E_S\to PU(\mathcal H_{\rho_S})
\]
be the Weyl-Heisenberg projective representation.  For
\(\omega\in\Omega^1_{A/k}\), define its fibrewise evaluation
\[
  \operatorname{ev}^1_S(\omega)=
  (\omega\otimes1)_P{}_{P\in S}\in V_S^*.
\]
The evaluated exact-one-form label space is
\[
  L_{\rm dR}(S)=\operatorname{ev}^1_S(dA)\subset 0\oplus V_S^*\subset E_S.
\]
Since \(0\oplus V_S^*\) is isotropic, the Weyl operators
\[
  \{W(0,\alpha):\alpha\in L_{\rm dR}(S)\}
\]
commute after the central phases are fixed.  Choosing eigenvalue phases then
defines a stabilizer subspace of \(\mathcal H_{\rho_S}\).

For explicit finite-field calculations one may use the standard coordinate
display.  With \(+1\) phases, define
\[
  A_S^{\rm dR}=
  \{v\in V_S:
    \prod_{P\in S}\psi_{K_P}(\alpha_P(v_P))=1
    \text{ for every }\alpha\in L_{\rm dR}(S)\}.
\]
In that coordinate display, the selected subspace is
\[
  \mathcal H_S^{\rm dR}=\ell^2(A_S^{\rm dR}).
\]
When all points are \(k=\mathbb F_p\)-rational, this is the simpler condition
\[
  \sum_{P\in S}\alpha_P(v_P)=0
  \qquad\text{for all }\alpha\in L_{\rm dR}(S).
\]

\subsection{Why This Is Compatible but Not Yet Full CSS}

The construction is compatible because exact one-forms evaluate to cotangent
vectors, and cotangent vectors are labels in \(T_PX\oplus T_P^*X\).  No basis of
\(\Omega^1_{A/k}\) is used as a list of physical qubits.

It is not yet the old full de Rham CSS rule.  A tangent-type label
\(w\in V_S\) commutes with all evaluated exact one-form labels exactly when
\[
  \alpha(w)=0\qquad\text{for every }\alpha\in L_{\rm dR}(S).
\]
Thus a choice of tangent labels commuting with \(L_{\rm dR}(S)\) is extra data.
Possible sources are a metric, Hodge star, action functional, correspondence,
or a deliberately chosen subspace of the annihilator.  Without such data, the
output is only the cotangent operator family and its stabilizer subspace.

\subsection{Example 1: A Reduced Point}

Let \(A=k=\mathbb F_3\).  Then \(\Omega^1_{k/k}=0\), \(V_P=0\), and
\[
  E_P=0,\qquad \dim_{\mathbb C}\mathcal H_{\rho_P}=1.
\]
Also \(dA=0\), so
\[
  L_{\rm dR}=0.
\]
The evaluated de Rham constraint does nothing, as it should.

\subsection{Example 2: Dual Numbers over F3}

Let
\[
  A=k[\epsilon]/(\epsilon^2),\qquad k=\mathbb F_3,\qquad P=(\epsilon).
\]
AQM-61 gives
\[
  V_P=k\,\partial_\epsilon,\qquad V_P^*=k\,d\epsilon,\qquad
  \dim_{\mathbb C}\mathcal H_{\rho_P}=3.
\]
For \(f=a+b\epsilon\),
\[
  df=b\,d\epsilon.
\]
Therefore
\[
  L_{\rm dR}=k\,d\epsilon=V_P^*.
\]
For \(v=r\partial_\epsilon\), \(d\epsilon(v)=r\).  The annihilator condition is
\(br=0\) for all \(b\in k\), hence \(r=0\).  Thus the selected stabilizer
subspace is one-dimensional; in the coordinate display it is
\[
  \mathcal H_P^{\rm dR}=\mathbb C|0\rangle.
\]
The old AQM-54 \(X\)-check is a Fourier-dual presentation of this prototype,
not the canonical tangent-coordinate reading.

\subsection{Example 3: The Rational Affine Line over F3}

Let \(X=\Spec k[x]\), \(k=\mathbb F_3\), and take rational support
\[
  S=\{0,1,2\}.
\]
AQM-62 gives \(V_S=k^3\) and
\[
  \dim_{\mathbb C}\mathcal H_{\rho_S}=3^3.
\]
The exact one-form \(df=f'(x)\,dx\) evaluates to
\[
  (f'(0),f'(1),f'(2))\in k^3=V_S^*.
\]
The three functions \(x,x^2,x^5\) give
\[
\begin{array}{c|c}
f & (f'(0),f'(1),f'(2))\\ \hline
x & (1,1,1)\\
x^2 & (0,2,1)\\
x^5 & (0,2,2).
\end{array}
\]
The determinant of these three rows is \(2\in\mathbb F_3^\times\), so
\[
  L_{\rm dR}(S)=V_S^*.
\]
Consequently the stabilizer subspace is one-dimensional; in coordinates it is
\[
  \mathcal H_S^{\rm dR}=\mathbb C|0,0,0\rangle.
\]

\subsection{Example 4: Two Independent Dual Directions}

Let
\[
  A=k[u,v]/(u^2,v^2),\qquad k=\mathbb F_3,\qquad P=(u,v).
\]
AQM-61 computes
\[
  V_P=k\,\partial_u\oplus k\,\partial_v,\qquad
  \dim_{\mathbb C}\mathcal H_{\rho_P}=3^2=9.
\]
The exact forms \(du\) and \(dv\) occur as \(d(u)\) and \(d(v)\).  Hence
\[
  L_{\rm dR}=k\,du\oplus k\,dv=V_P^*.
\]
The annihilator is zero, so the stabilizer subspace is one-dimensional; in
coordinates it is \(\mathbb C|0,0\rangle\).

\subsection{Example 5: The Crossing over F3}

Let \(A=k[x,y]/(xy)\) and take the rational support.  AQM-62 gives one
two-dimensional tangent at the origin and four one-dimensional tangents away
from it:
\[
  V_S\cong k^6,\qquad
  \dim_{\mathbb C}\mathcal H_{\rho_S}=3^6.
\]
A function on the crossing is represented by an \(x\)-axis polynomial and a
\(y\)-axis polynomial with the same constant term.  Its differential along the
\(x\)-axis gives arbitrary derivative values at \(x=0,1,2\), by Example 3.
Its differential along the \(y\)-axis gives arbitrary derivative values at
\(y=0,1,2\), again by Example 3.  These six derivatives are exactly the six
cotangent coordinates dual to \(V_S\).  Hence
\[
  L_{\rm dR}(S)=V_S^*.
\]
The stabilizer subspace is one-dimensional; in coordinates it is
\[
  \mathbb C|0,0,0,0,0,0\rangle.
\]

\subsection{Example 6: The Circle over F3}

Let
\[
  C=\Spec k[x,y]/(x^2+y^2-1),\qquad k=\mathbb F_3.
\]
Use the rational point order
\[
  (1,0),(2,0),(0,1),(0,2).
\]
AQM-62 gives \(V_S\cong k^4\) and
\[
  \dim_{\mathbb C}\mathcal H_{\rho_S}=3^4.
\]
Use tangent bases \((0,1)\) at the first two points and \((1,0)\) at the last
two.  The following exact forms evaluate to cotangent rows:
\[
\begin{array}{c|c}
f & (df_P(\tau_P))_{P\in S}\\ \hline
y & (1,1,0,0)\\
x & (0,0,1,1)\\
xy & (1,2,1,2)\\
xy^3 & (0,0,1,2).
\end{array}
\]
The determinant of this \(4\times4\) row matrix is \(2\), so the rows span
\[
  V_S^*.
\]
Thus \(L_{\rm dR}(S)=V_S^*\), and the stabilizer subspace is one-dimensional;
in coordinates it is \(\mathbb C|0,0,0,0\rangle\).

\subsection{Conclusion}

The compatibility result is positive but austere:
\[
  dA\quad\leadsto\quad L_{\rm dR}(S)\subset
  \bigoplus_{P\in S}T_P^*X
\]
is a canonical cotangent-label construction inside the tangent-cotangent Weyl
projective representation.  The examples show that using all exact forms on a
finite rational support often gives
\[
  L_{\rm dR}(S)=V_S^*
\]
and therefore a one-dimensional stabilizer subspace.  A nontrivial dynamical
principle must restrict the allowed exact forms, add an action/Hamiltonian, or
choose additional tangent-type constraints from a justified source.  The old de
Rham CSS test is therefore compatible only after this fibrewise
reinterpretation, and it remains a prototype rather than a final dynamics.
